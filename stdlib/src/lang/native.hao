// ============================================================
//  lang 包 —— 运行时底座 extern（禁串业务 FFI）
// ============================================================
package lang;

/* 仅对象分配底座；String.fromUtf8 写 value/cpLen 字段 */
extern func hao_object_new(nfields: Long, bitmap: Long): String = "hao_object_new";
