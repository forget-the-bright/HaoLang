# 方法论（设计理想）

> 入口索引。职责总表与批末落盘见 [`../文档治理.md`](../文档治理.md)。
>
> **冲突优先级（方法论优先）**：原则 / 禁止 / 验收以**本目录五份方法论**为准；[`../IR与GC契约.md`](../IR与GC契约.md) 写落地细节与可观测口径；[`../../记忆文档.md`](../../记忆文档.md) 第 5 章已定决策与方法论冲突时，须在方法论旁注「已做（等价）/ ARCH」后再改契约，**禁止**用契约空「仍开」盖过已交付切片。物理禁令（SuspendThread / 移动式 GC / incomplete→0 当绿）与永久 C 底座标 **ARCH**，不开伪任务。示例函数名、示例 API **非强制实现**。

| 文档 | 一句话 |
|------|--------|
| [`调试能力&语言架构设计方法论与约束规范.md`](调试能力&语言架构设计方法论与约束规范.md) | 语言分层、统一收口、调试约束 |
| [`高级语言GC设计全覆盖检查清单.md`](高级语言GC设计全覆盖检查清单.md) | 工业 GC 能力全覆盖（理想 / 差距分析） |
| [`FFI与运行时分层隔离.md`](FFI与运行时分层隔离.md) | 去 C 化、最小 C 底座、FFI 边界隔离 |
| [`HaoLang ABI 设计方法论 + 内存托管权责规范.md`](HaoLang%20ABI%20设计方法论%20+%20内存托管权责规范.md) | **ABI ≠ 归属**；拷贝 vs 句柄代理；禁堆内指针出桥 |

**语言侧已落规范（非方法论理想）**：四属性与 `TypeKind` 对照见 [`../类型属性.md`](../类型属性.md)；用户短节 [`../hao语法.md`](../hao语法.md) §4.0。资源 FFI（v0.55.61+）：仅**拷贝语义 | NativeHandle 代理**；**ThreadPool** 已切 Handle（门禁 `handle_resource_gate`）。

**本 epic（METH）已交付切片**：Pool Handle、IRGen TRACE 统一 helper、全量 Unwind↔GC 关勾、barrier IR 属性、非循环 spill 收窄、concurrent sweep + exact-size freelist（v0.66）、**G2 Sema 首刀**（`resolveType`→`src/sema`）。下一切口见坑债延期表 / 记忆第 10 章（默认 **F 增量编译**）。
