# 项目时间线 · v0.50.x ～

> 同十位大版本合订；细节坑见 [`../坑债.md`](../坑债.md)。
>
> 索引：[`索引.md`](索引.md)

### v0.57.1（2026-08-12）· Map 接口化 + JSON 去 array C

> **目标**：对标 Java `Map`；JSON/`for-in` 走 `keySet`/`entrySet`/`values` 视图；业务 `.hao` 禁 `hao_array_*`。
> **改**：
> - [`MapTypes.hao`](../../stdlib/src/collections/MapTypes.hao)：`interface Map` / `MapEntry`；`class Map`→`HashMap`；LHM/TreeMap/CHM `: Map`
> - 视图返回 `Iterable`（实现 `ArrayIterable` 快照）；`Iterable.toArray()` 供嵌套泛型 for-in 稳妥出口
> - [`JSON.hao`](../../stdlib/src/json/JSON.hao)：删 `_jsonArr*`；具体 Map 重载 `keys()+get`；Object 路径 `keySet` 反射；List 仍 `size+get`
> - 门禁：`p5_smoke` 黑名单 stdlib `*.hao` 的 `hao_array_*`；`target/p8-smoke/p8_all.hao`

**验收**：`p5_smoke` → `P5_SMOKE+IR_SYNC+P7+P8 OK`；`test.sh --rebuild-all`；`negcheck` 28/28；`hao version`=0.57.1。

### v0.57.0（2026-08-11）· P7 激进扩面：println/hash/IR串/float/reflect/STW·thread

> **纪律**：各子项先独立 `target/p7-smoke` 全绿，再通用套件；改 ABI 同步 IR；禁软着陆。

| 子项 | 改动 |
|------|------|
| **P7a** | 标量 println→toStr；删 `runtime_hash.c`；channel 用 `reflect.ptrOfStr`；`hao_str_data` 白名单；清 `script/_*.py` |
| **P7b** | IR `+`/`.length`/`s[i]`→`@lang$String.concat`/`codePointLen`/`charAtCp`；布局 C 仍底 |
| **P7c** | `runtime_float.c` 自研 parse/toStr；reflect field 同路径；禁 libc `strto*`/`%g` |
| **P7d** | invoke 槽 ABI 写死；业务 `ptrOf*` 白名单门禁 |
| **P7e** | soft-STW 热 miss 宽限（`stwGraceRescues`）；`hao_thread_start/join`→`NativeHandle`（IR `ptr`） |

**验收**：`p5_smoke.ps1` → `P5_SMOKE+IR_SYNC+P7 OK`；`test.sh --rebuild-all` **1029**/0；`negcheck` **28/28**；`hao version`=0.57.0。

### v0.56.3（2026-08-11）· P6：删 `hao_array_get_ptr` + cstr 白名单门禁

> **目标**：按 FFI/ABI 方法论清掉废弃 i64 藏针皮带；`hao_str_cstr` 调用点仅允许定义 + `hao_ffi_dup_*`。
> **改**：删 [`runtime_array.c`](../../stdlib/runtime_array.c) `hao_array_get_ptr`；header 声明 `hao_array_get_obj`；`p5_smoke` 黑名单/cstr 白名单；类型属性 §7.2。

**验收**：`powershell -File script/win/p5_smoke.ps1` → `P5_SMOKE+IR_SYNC OK`；`hao version`=0.56.3。

### v0.56.2（2026-08-11）· IR↔Handle 同步铁律 + 门禁

> **原则**：改 stdlib Handle/已删 C 必须同步 `src/irgen`（v0.56.1 反射漏同步教训）。
> **改**：`emitClassOfMetaFromRawMeta` 唯一合成入口；注解 `classMeta` 注明裸表+读侧 wrap；FFI/类型属性写铁律；`p5_smoke.ps1` 加 emit `.ll` 黑名单/toStr 白名单。

**验收**：`powershell -File script/win/p5_smoke.ps1` → `P5_SMOKE+IR_SYNC OK`。

### v0.56.1（2026-08-11）· 修 TypeName.Class 裸 meta 当 Handle

> **根因**：P5 将 `classOfMeta` 入参改为 `NativeHandle` 后，IR 仍 `classOfMeta(@T.meta)`，把裸 `HaoClassMeta*` 当句柄 → `TypeName.Class` 单例/注解比对/`newInstance` 全坏。
> **修**：`staticinit` 改为 `classOfMeta(hao_handle_wrap(@T.meta))`；独立冒烟 `target/p5-smoke/p5_lib_smoke.hao`。

**验收**：`p5_lib_smoke` → `P5_SMOKE OK`；`hao version`=0.56.1。

### v0.56.0（2026-08-11）· P5 激进清零：无 Long? 藏针 + 最小 C

> **目标**：TYPE-ATTR 结案——stdlib 禁 `Long?` 藏针；能 Hao 必 Hao 并删 C；出桥强制 dup；永久底座写死理由（禁「可后挪」）。

- **Handle**：reflect `Class.meta` + 全部 reflect extern → `NativeHandle`（永生 meta `drop=NULL`）；sync Mutex/atomic*/CHM → `hao_sync_cell_new`；Mutex.lock/unlock Hao CAS + safepoint；删 `hao_sync_lock/unlock`。
- **串**：substring（`byte_of_cp`+`byte_slice`）、byteIndexOf、char_to_str → Hao；删对应 C；charAt/compare 用 `s[i]`。
- **parse/toStr**：整型布尔上移；IR→`@lang$Integer.toStr` 等；float 留 libc + **强制 dup**；field_set 用文件内静态解析。
- **time/hash/assignable**：civil 历法+format、非浮点 hash、`isAssignableFrom` → Hao；删 C。
- **文档**：类型属性 §7 永久底座；坑债 TYPE-ATTR 关闭；套件基线 **1027**。

**验收**：`test.sh --rebuild-all` **1027**/0；`negcheck` **28/28**；`hao version`=0.56.0。

### v0.55.62（2026-08-11）· P4 IO 上移 + channel NativeHandle

> **目标**：TYPE-ATTR P4——`os.readFile/writeFile/exists` 纯 Hao（经 FileStream）；删除对应 C；channel 补铁律切 Handle。

- **Hao**：[`os.hao`](../../stdlib/src/os/os.hao) 三 API 组合 FileStream；[`FileStream.readAll`/`writeAll`](../../stdlib/src/os/FileStream.hao)。
- **C**：删 `hao_os_readfile|writefile|exists`；保留 getenv/system/remove。
- **channel**：`Long?` → `NativeHandle`；`hao_chan_*` 经 `hao_handle_attach`；anti-remake 保留。
- **文档**：类型属性 P4 ✅；坑债 TYPE-ATTR；FFI 注明 channel Handle。

**验收**：`test.sh --rebuild-all` **1029**/0；`negcheck` **28/28**；无上述三符号；`hao version`=0.55.62。

### v0.55.61（2026-08-11）· NativeHandle + FFI 边界收口（拷贝 | 句柄）

> **目标**：激进落地 ABI/托管权铁律——边界仅拷贝语义与 `NativeHandle` 代理；消灭资源路径 `Long?` 藏针与对外「堆内 cstr」模式。

- **`NativeHandle`**：`runtime_handle.c`（OPAQUE + finalizer；代理释放）；`lang/NativeHandle.hao`；`hao_ffi_dup_cstr`/`dup_bytes`。
- **切面**：fs / net / regex 上层只见 Handle；删 `int64_t*` unit 藏针签名。
- **出桥**：fopen/fwrite/send/print/time/regex 等 libc 入参经 dup；`hao_str_cstr`/`data` 桥私有+审计清单。
- **文档**：方法论索引 ABI 权责文；FFI/类型属性 §5 **已执行**；坑债 TYPE-ATTR P3 ✅。
- **不动**：sync 原子 / reflect meta / channel 的 `Long?`；String 布局；P4 全量 IO 上移。

**验收**：`test.sh --rebuild-all` **1029**/0；`negcheck` **28/28**；`hao version`=0.55.61。

### v0.55.60（2026-08-11）· String UTF-8 `[Byte]` 分体 + P2b 算法上移

> **目标**：一次切透内建 String 布局（对标 Java 9 `byte[]` 载荷，仅 UTF-8）；业务算法上移 Hao；禁止长期双 ABI。

- **布局**：`HaoString{bytes→[Byte], cp_len}`，`hao_object_new(2, bitmap=1)`；载荷 `hao_array_new` esz=1，cap≥len+1 尾 NUL；删旧 `{len,cap,data[]}` OPAQUE。
- **C 底座**：工厂 / `eq` / `concat` / `substring` / `char_at` / `cstr`/`byte_len`/`get_bytes`/`from_byte_arr`；os/net/fs/print/hash/regex/time 改走 `hao_str_*`。
- **Hao**：`indexOf*`/`lastIndexOf`/`trim`/`toUpperCase`/`toLowerCase`/`compare`；新增 `byteLength`/`getBytes`/`fromBytes`。
- **套件**：`string_char` +4 → 基线 **1029**。

**验收**：`test.sh --rebuild-all` **1029**/0；`negcheck` **28/28**；`hao version`=0.55.60。

### v0.55.59（2026-08-11）· P2a 串查询组合上移 Hao

> **目标**：按 [`类型属性.md`](../类型属性.md) P2a，把 `isEmpty`/`contains`/`startsWith`/`endsWith` 改为 Hao 组合；删除对应 C 薄函数；补套件。

- [`String.hao`](../../stdlib/src/lang/String.hao)：四方法用 `.length` / `substring` / `indexOf` / `==`；[`native.hao`](../../stdlib/src/lang/native.hao) 删四 extern。
- [`runtime_string.c`](../../stdlib/runtime_string.c)：删 `hao_str_is_empty`/`contains`/`starts_with`/`ends_with`。
- 套件 `string_char` +10 行 → 基线 **1025**。

**验收**：`test.sh --rebuild-all` **1025**/0；`negcheck` **28/28**；`hao version`=0.55.59。

### v0.55.58（2026-08-11）· 类型属性一等 API + P1 盘点

> **目标**：按 [`类型属性.md`](../类型属性.md) 比对代码后，交付编译器一等属性 API，并写入可上移 API 盘点与后续开发序；**不上移** String/容器、不做 NativeHandle。

- `Type::isValueType` / `isGcManaged` / `isUnsafePointer`（[`Type.h`](../../src/sema/Type.h)）；`IRGen::isGcPointerType` → `isGcManaged`。
- [`类型属性.md`](../类型属性.md)：§6 比对结论、§7 P1 盘点、§8 开发计划；坑债 TYPE-ATTR 更新。

**验收**：`test.sh --rebuild-all` **1015**/0；`negcheck` **28/28**；`hao version`=0.55.58。

### v0.55.57（2026-08-11）· 语言类型属性规范（纯文档）

> **目标**：把「值 / 引用 / 裸指针」写入语言规范；单独落地四属性对照表；写清 String/容器去 C 化与 FFI「只拷贝+句柄」路线；**不改**编译器/运行时代码。

- 新建 [`类型属性.md`](../类型属性.md)：`GcManaged` / `IsReference` / `IsValueType` / `IsUnsafePointer` ↔ `TypeKind`；标隐式 vs 一等；数组/String/`T?`；FFI 边界；去 C 化 P0～P4。
- [`hao语法.md`](../hao语法.md) §4.0 三元分类短节；对标表补值/引用与 `int[]` 行。
- 文档治理地图 + 方法论 README + FFI 文首指针；记忆 §5.17；坑债将就债 **TYPE-ATTR**。

**验收**：链接可点；`hao version`=0.55.57；套件基线仍 **1015**（DOC-only）。

### v0.55.56（2026-08-11）· 文档全局治理（中度）

> **目标**：去重文档地图、理顺方法论入口与时间线索引、清过期能力快照；无运行时行为变更。

- 文档地图完整表**仅**留 [`文档治理.md`](../文档治理.md)；README / 记忆改为短指针。
- 方法论：[`方法论/README.md`](../方法论/README.md)；FFI 文短名 [`FFI与运行时分层隔离.md`](../方法论/FFI与运行时分层隔离.md)；冲突时契约 + §5 优先。
- 时间线索引严格从旧到新分段；语法/契约标题/速查层不再钉死旧补丁号作「当前能力」。

**验收**：链接可点；`hao version`=0.55.56；套件基线仍 **1015**（DOC-only）。

### v0.55.55（2026-08-11）· monitor 50RPS 残留 CRT（time + STW tid）

> **目标**：50RPS 压测脚本下仍 `movdqa` AV 时补齐残留 CRT；不把根目录旧 exe / getClass loc 当 UAF。

- `hao_time_format`、`gc_record_stw_incomplete` tid 列表去 `snprintf`；`getclass` 堆校验。
- 验收：新编 monitor + 50RPS ≥2 分钟；套件基线仍 **1015**。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1015**/0；`hao version`=0.55.55。

### v0.55.54（2026-08-11）· 08-gc-monitor 压测 AV（Win64 栈对齐）

> **目标**：钉死并薄修压测 `av_addr=-1`（CRT `movdqa`）；不把 incomplete→0 / UAF 误判当绿。

- 根因：子线程 RSP 未 16B 对齐 → libcmt `snprintf`/`movdqa` AV；`HAO_GC_TRACE` 放大。
- 修：IR **S128** datalayout；int→str 手写；线程入口对齐 trampoline；Win64 `hao_trace` 避 CRT。
- 套件基线仍 **1015**（无新 assert 行）。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1015**/0；手压 monitor ≥1–2 分钟；`hao version`=0.55.54。

### v0.55.53（2026-08-11）· 定位发现十九期 S2 缺 park tid/龄

> **目标**：复压一眼能说出缺 park 是谁（tid）+ 多久未 SP；不把 incomplete→0 / §18.2④ 全文当绿。

- S2a/S2b：末次 miss tid 列表 + safepoint 龄 + TRACE/stats/monitor。
- F1：finalizer skip_abort / live_at_sweep / diag 发现面。
- S2e：**判定跳过**补丁（根握手税；身份已可查）。
- 套件基线 **1015**（gcapi +1）。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1015**/0；`hao version`=0.55.53。

### v0.55.52（2026-08-11）· 定位发现十八期 S1 STW abort 分相

> **目标**：压测 `stwIncomplete`/`markAbort` 同步涨可分相定位；不把 incomplete→0 当绿。

- S1：分相计数 + 末次快照 + `HAO_GC_TRACE` + monitor；S1e **判定跳过**（根握手税 / 工程债仍开）。
- 套件基线 **1014**（gcapi +3）。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1014**/0；`hao version`=0.55.52。

### v0.55.51（2026-08-11）· 定位发现十七期 A15/D17/U19/L17

> **目标**：leave noop / 非池 acquire TRACE；`new` 字段默认薄 DI；for-in-catch 根冒烟。

- A15：`HAO_IRGEN_TRACE` → `leave_spill noop=1` / `acquire_spill pool=0`；非 TRACE 全收口（统一 helper 仍开）。
- D17：`new`/reflect `$new` 字段默认薄 `dbg.value`；完整类型仍开。
- U19：`gc_try_for_in_catch_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.51。

### v0.55.50（2026-08-11）· 定位发现十六期 A14/D16/U18/L16

> **目标**：note_block/unwind_gc TRACE；lambda 捕获薄 DI；select-in-catch 根冒烟。

- A14：`HAO_IRGEN_TRACE` → `hao:irgen:note_block` / `unwind_gc`；非 TRACE 全收口。
- D16：lambda 捕获 unpack 薄 `dbg.declare`/`dbg.value`（含 `capturesThis`）。
- U18：`gc_try_select_in_catch_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.50。

### v0.55.49（2026-08-11）· 定位发现十五期 A13/D15/U17/V11/L15

> **目标**：unwind/catchDepth TRACE；fn/this 初值薄 dbg.value；catch 内 finally-throw 根冒烟；外部根 VERIFY 毒针。

- A13：`HAO_IRGEN_TRACE` → `hao:irgen:unwind` / `catch_enter` / `catch_leave` / `gc_unwind`；非 TRACE 全收口。
- D15：顶层/方法/ctor 形参与 `this` 初值薄 `dbg.value`。
- U17：`gc_try_catch_finally_throw_root_smoke`（`catchDepth_`×reason=4）；**全量** Unwind 仍开。
- V11：`hao_debug_poison_ext_root`；fatal `ext_i=`；**非**跨线程 VERIFY。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.49。

### v0.55.48（2026-08-11）· 定位发现十四期 A12/D13/D14/U16/L14

> **目标**：池扩容 grow_spill TRACE；lambda 参数 / select 绑定薄 DI；catch 体内嵌套 try 根冒烟。

- A12：`HAO_IRGEN_TRACE` → `hao:irgen:grow_spill`；spill 门禁；非 TRACE 全收口。
- D13：lambda 形参薄 `dbg.declare`/`dbg.value`。
- D14：select 接收绑定薄 DI。
- U16：`gc_try_nested_catch_root_smoke`（`catchDepth_`）；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.48。

### v0.55.47（2026-08-11）· 定位发现十三期 A11/D12/U14/U15/L13

> **目标**：stickyFloor/块作用域 TRACE；catch 绑定薄 dbg.value；haoroutine×try 与 when×for×try 组合根冒烟。

- A11：`HAO_IRGEN_TRACE` → `hao:irgen:sticky_floor` / `block_enter` / `block_leave`。
- D12：catch 绑定薄 `dbg.value`。
- U14：`gc_try_haoroutine_finally_root_smoke`；**全量** Unwind 仍开。
- U15：`gc_try_when_for_finally_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.47。

### v0.55.46（2026-08-11）· 定位发现十二期 A10/D11/U12/U13/L12

> **目标**：pin/enter TRACE；for 迭代薄 dbg.value；构造函数 / 静态方法 try 根冒烟。

- A10：`HAO_IRGEN_TRACE` → `hao:irgen:pin_spill` / `enter_spill`；spill 门禁。
- D11：for 循环变量每轮迭代薄 `dbg.value`。
- U12：`gc_try_ctor_finally_root_smoke`；**全量** Unwind 仍开。
- U13：`gc_try_static_method_finally_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.46。

### v0.55.45（2026-08-11）· 定位发现十一期 D10/A9/V10/U11/L11

> **目标**：字段复合薄 dbg.value；unpin/leave TRACE；C 叶 VERIFY 毒针发现面；catch-only 根冒烟。

- D10：实例/静态字段复合写回薄 `dbg.value`。
- A9：`HAO_IRGEN_TRACE` → `hao:irgen:unpin_spill` / `leave_spill`；spill 门禁。
- V10：VERIFY C 叶仅毒针；`hao_debug_poison_c_leaf`；fatal `leaf_i=`/`ptr=`；**非**真实叶全扫。
- U11：`gc_try_catch_only_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.45。

### v0.55.44（2026-08-11）· 定位发现十期 D9/V9/A8/U10/L10

> **目标**：下标薄 dbg.value；GPR 溅射 VERIFY；recycle TRACE；类方法 try 根冒烟。

- D9：`a[i]=` / 复合下标写回薄 `dbg.value`。
- V9：VERIFY 扫 `g_gpr_spill`；`hao_debug_poison_gpr`；fatal `gpr_i=`/`ptr=`；非堆整数跳过。
- A8：`HAO_IRGEN_TRACE` → `hao:irgen:recycle_spill`；spill 门禁。
- U10：`gc_try_method_finally_root_smoke`；**全量** Unwind 仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.44。

### v0.55.43（2026-08-11）· 定位发现九期 V8/D7/D8/A7/L9

> **目标**：refl_i64 VERIFY 槽定位；复合/字段薄 `dbg.value`；acquire TRACE 扩一面。

- V8：VERIFY 扫 `g_refl_i64_pins`；`hao_debug_poison_refl_i64`；fatal `refl_i64_i=`/`ptr=`。
- D7：简单变量复合赋值薄 `dbg.value`。
- D8：实例/静态字段 `=` 薄 `dbg.value`。
- A7：`HAO_IRGEN_TRACE` → `hao:irgen:acquire_spill`；spill 门禁。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.43。

### v0.55.42（2026-08-11）· 定位发现八期 U8/V7/D6/L8

> **目标**：修 Unit-lambda×try `ret` 串 main；remset VERIFY 槽定位；赋值薄 `dbg.value`；直测关 U7a 规避债。

- U8：`emitLambdaImpl` 保存/清 `inMain_`；loc_smoke Unit-lambda×try `ret void` + 链接。
- U8b：`gc_try_unit_lambda_finally_root_smoke`（lambda 体内直接 try）；并入 spill。
- V7：VERIFY 扫 remset；`hao_debug_poison_remset`；fatal `remset_i=`/`ptr=`。
- D6：简单局部赋值薄 `dbg.value`（≥2）；完整 Hao 类型仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.42。

### v0.55.41（2026-08-11）· 定位发现七期 U7/V6/D5/L7

> **目标**：线程×try / 再抛多层 / for×break 根发现；VERIFY pin 槽定位；薄 `dbg.value` 可门禁。

- U7a：`gc_try_thread_finally_root_smoke`（worker 内 try/finally + collect；具名 worker 规避 Unit-lambda×try IR 坑）。
- U7b：`gc_try_rethrow_multilayer_root_smoke`（≥3 层 finally；catch 再 throw；for×break×finally）。
- V6：VERIFY 扫 scan pin；`hao_debug_poison_scan_pin`；fatal 含 `pin_i=`/`ptr=`。
- D5：`-g` 局部初值薄 `llvm.dbg.value`；完整 Hao 类型视图仍开。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.41。

### v0.55.40（2026-08-11）· 定位发现六期 U6/V5/A6/L6

> **目标**：控制面盲区根发现；VERIFY 槽级定位；清槽 TRACE 正路径；DOC 诚实。

- U6：`gc_try_break_finally_root_smoke` + `gc_try_lambda_finally_root_smoke`；并入 spill。
- V5：VERIFY fatal 含 `shadow_i=`/`ptr=`；loc_smoke 断言。
- A6：`HAO_IRGEN_TRACE=1` → `hao:irgen:clear_spill base=…`；spill 门禁。
- L6：DOC 同步；全量 Unwind / `dbg.value` / Statepoint / G2 / 语句级清槽 / 功能轨 **仍开**。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.40。

### v0.55.39（2026-08-11）· 定位发现五期 U5/A5/V4/L5

> **目标**：控制面 try 根发现；清槽 underflow 可观测/STRICT；VERIFY 重入跳过计数；select `!dbg`。

- U5：`gc_try_control_root_smoke`（return / finally 内 throw / select×try / when×try）；finally 内 throw 后禁误清 `blockTerminated_`。
- A5：`clearLoopSpillSlots` 对 `target < base` 打 `hao:irgen:clear_spill_underflow`；`HAO_IRGEN_STRICT=1` 记诊断。
- V4：`hao_gc_verify_skip_reenter` / metrics `verifySkipReenter`。
- L5：select 调度 `switch` 附 `!dbg`；DOC 诚实同步（全量 Unwind / `dbg.value` / Statepoint / G2 仍开）。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.39。

### v0.55.38（2026-08-11）· 定位发现四期 U4/V3/D4

> **目标**：嵌套 try/for 根发现；VERIFY 覆盖分配 collect；catch/for 薄 declare + Except `!dbg`。

- U4：`gc_try_nested_finally_root_smoke`（try×try×finally + for×try continue）。
- V3：`gc_run_collect_locked` trampoline 外 VERIFY；loc_smoke `verify_alloc`（poison→分配压力）。
- D4：catch/for `dbg.declare`；Except `setjmp`/`switch` 附 `!dbg`。

**验收**：`loc_smoke`+`spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.38。

### v0.55.37（2026-08-11）· 定位加深三期 U3/V2/D3

> **目标**：多层 try×循环根发现；VERIFY 坏根可定位；薄 `dbg.declare`（非完整类型视图）。

- U3：`gc_try_loop_finally_root_smoke`（含 continue 穿 finally）；修 cleanup 跳转前未清 `unwindReason` 死循环。
- V2：`hao_debug_poison_shadow_root` + loc_smoke；VERIFY 在 trampoline **外**前/后扫；坑：裸垫片栈未对齐致 CRT fatal AV。
- D3：`-g` 参数/直接 varDecl → `llvm.dbg.declare` + `DILocalVariable`。

**验收**：`loc_smoke` + `spill_ir_smoke` ALL PASS；`test.sh --rebuild-all` **1011**/0；`hao version`=0.55.37。

### v0.55.36（2026-08-11）· 定位门禁续作 C0～T1

> **目标**：门禁入 `test.sh`；try/finally 根冒烟；`HAO_GC_VERIFY`；dbg 栈 TLS。

- C0：loc/spill 脚本挂主路径；U2：`gc_try_finally_root_smoke`；H1：VERIFY；T1：帧表 TLS。

**验收**：`test.sh --rebuild-all` 含两脚本；套件 **1011**/0。

### v0.55.35（2026-08-11）· 定位发现续作 D0～U1

> **目标**：问题收集面 + `src=` 与真因对齐 + 栈可读 + spill IR 门禁。

- D0/L1/L2/U1：`loc_smoke`；调用点钉 loc；`[lib]`；throw 可定位。
- L3：`spill_ir_smoke`；block smoke 仍绿。

**验收**：两脚本 ALL PASS；套件 **1011**/0。

### v0.55.34（2026-08-10）· 嵌套 while sticky 分层

> **目标**：清 `gc_block_scope_root_smoke` 多轮 AV。

- 根因：内层 cond `sticky.size()>1` 误剥外层 → `unpin` 杀块内 List 池槽。
- 修复：`stickyFloor` / `stickyEnter`；详见坑债 **CU**。

**验收**：smoke true×3；套件 **1011**/0。

### v0.55.33（2026-08-10）· 定位加深 P0～P3 + G1 口径

> **目标**：问题可复现、可定位；套件双门禁；作用域根机口径冻结。

- P0 `loc_smoke`；P1 `stack=`；P2 Resolve 带路径；P3 行数硬卡 1011。
- G1：块/循环作用域机确认；expr 清槽后置；block 压力冒烟记债。

**验收**：`loc_smoke` + 套件 **1011**/0。

### v0.55.32（2026-08-10）· 定位底座 L0 + R4L/R5/R3/D1/DOC

> **目标**：编译/运行都能精确定位；收完方法论剩余可勾项（G1/G2 仍后置）。

- L0：Diagnostic 带路径；panic `src=`；命令文档读 log。
- R4L/R5/R3：POSIX 崩溃钩子；OOM fatal；metrics 表。
- D1：Driver `toolError`；DOC/I2+ 口径校正。

**验收**：套件退出码 **0**；冒烟见坑债 **CS**。

### v0.55.31（2026-08-10）· dbg 方法论对照补齐

> **目标**：按函数 DISubprogram；方法/lambda 填 loc；`-g` 写入命令文档。

- `beginDebugFunction` / `beginDebugSubprogram`；Location.scope 按函数。
- [`hao命令.md`](../hao命令.md) 文档化 `-g`。

**验收**：套件 **1011** / **0**。

### v0.55.30（2026-08-10）· I0/I3 + 薄 I4

> **目标**：SourceLoc + IROps 挂 `!dbg`；CLI `-g`；默认关。

- `SourceLoc` / `locFrom`；关键 gen* 填 loc。
- `IROps::emitInst` 行尾 `, !dbg !N`；`finish` 最小 DI（CU/File/SP/Location）。
- `-g`：IR 元数据 + clang `-g`；无 flag 行为同 0.55.29。

**验收**：套件 **1011** / **0**。下一批：DI 精修或功能。

### v0.55.29（2026-08-10）· IROps 扩 API + I5 收口

> **目标**：扩 cast/gep/select/fcmp/…；清同类指令裸 emit；钉死「不必零 emit」口径。

- 新增 `emitFCmp/Select/Cast/Gep/PtrCast/ExtractValue/FNeg/CallTyped`；Expr/Value/… 可封装路径 **0** 裸 `em_.emit`。
- **允许残留**：Control/Except 的 switch、setjmp+#0；IROps 实现体；Label/Raw/脚手架。
- 口径：同类收口，非消灭所有字符串 emit。

**验收**：套件 **1011** / **0**。下一批：I0/I3（loc+`!dbg`）。

### v0.55.28（2026-08-10）· I5 复查补遗 + Class/Lambda

> **目标**：回头验批4 遗漏；迁完 Class/Lambda/函数顶层。

- 补 Except/Literal 可封装路径；批5：Class **45→27**、Lambda **24→3**、IRGen.cpp **17→7**。
- 残留主因：gep/cast/switch/setjmp/phi/间接 call（待 IROps 扩 API）。

**验收**：套件 **1011** / **0**。下一批：Expr 残留 / `emitCast|Gep|Select` 扩 IROps → I0/I3。

### v0.55.27（2026-08-10）· IROps 收口 + I5 批4

> **目标**：指令层与 GC/语法层解耦；Literal/Except 迁封装。

- **IROps**：通用文本 IR 指令收口；`IRGen` 持有 `ops_` 并转发；GC 走 `ops_`。
- **批4**：Literal **78→14**；Except **66→24**。
- 语法文件拆分保留；`!dbg` 仍挂 IROps（Phase C）。

**验收**：套件 **1011** / **0**。下一批：Class/Lambda + Expr 残留。

### v0.55.26（2026-08-10）· I5 批3 Expr

> **目标**：表达式路径迁指令封装。

- `IRGenExpr`：`em_.emit` **186→83**；br/load/store/runtime call/binop 主路径已走封装。
- 残留：多行 phi、三元 pred icmp、gep/cast/fcmp、虚调用间接 call。

**验收**：套件 **1011** / **0**。下一批：I5 批4 Literal/Except。

### v0.55.25（2026-08-10）· I1b + I5 批1/批2

> **目标**：指令封装补齐 BinOp/ICmp/Phi/Alloca；迁移 Value + Control 高频路径。

- **I1b**：`emitAlloca*` / `emitBinOp` / `emitICmp` / `emitPhi` / `emitUnreachable`。
- **I5 批1**：`IRGenValue` GC/spill/box/toString/除零等。
- **I5 批2**：`IRGenControl` br/load/store/icmp/call 主路径；踩坑：烧 SSA 号、`""+v+""` 误替换。

**验收**：套件 **1011** / 退出码 **0**。下一批：I5 批3 Expr（~186 处裸 emit）。

### v0.55.24（2026-08-10）· 定位优先 Phase A 收口 + I1a 骨架

> **目标**：可观测地基：Trace/Assert → fatal/崩溃 GC 快照 → safepoint IR 单入口；再启指令封装。

- **R1+R2**：`hao_trace`/`HAO_ASSERT`；GC 冷路径埋点（collect enter / safepoint park）。
- **R4'**：`hao_report_fatal` + panic 同源；Win UEF 写 `hao_gc_fprint_debug_snapshot`。
- **I2**：`emitSafepoint()`；消灭 10 处裸 `call void @hao_gc_safepoint`。
- **I1a**：`emitLoad/Store/Call/Br/CondBr/Ret`；GC 封装改走通用指令；旧 gen* 裸 emit = I5 迁移债。

**验收**：套件 **1011** / 退出码 **0**。下一批：I5 分批迁移高频路径。

### v0.55.23（2026-08-10）· JSON Map 键 + Http Content-Length

> **目标**：修套件回归——`jsonTest` Map stringify AV；`ioHttpTest` POST body/`cl!!` panic。

- **JSON**：`[String]` 键用 `_jsonArrGetStr`（禁 Object 虚调用）；`looksLikeMap/List` 对齐 Html；`stringifyObj` 键快照。
- **Http**：`contentLengthOfLine` 独立函数帧，避开 while spill/sticky 清 `line` 根。

**验收**：套件 **1011** / 退出码 **0**。

### v0.55.22（2026-08-10）· invokeObj 对标 Java Method.invoke

> **目标**：不将就——引用返回不得藏进 `Long`；对标 Java `Method.invoke → Object`。

- **API**：`Class.invokeObj` / `hao_reflect_invoke_obj`；`invoke():Long` **仅**非可空标量，否则 panic。
- **stdlib**：Mvc/JSON/Html 引用路径改 `invokeObj`；`hao_array_get_obj` 取数组元素。
- **皮带**：`refl_i64_pin` 仍服务 `array_get_ptr`/`objOf` 旧路径。

**验收**：monitor 压测约 1 天稳定、峰值约 10MiB；`engine` 含 v0.55.22；套件 `v20` 标量 invoke。

### v0.55.21（2026-08-10）· reflect invoke i64 藏指针钉住

> **目标**：修 v0.55.20 `field_get 非对象/已回收`（GcSnapshot 在 JSON 前被 sweep）。

- **根因**：`invoke`→`Long` 藏指针不算 GC 根；`coerceResult` 先做 `retType` 字符串再 `objOf`。
- **runtime**：TLS `refl_i64_pin`（STW 扫）；`invoke` 钉、`inttoptr`/`strOfInt` 卸；`array_get_ptr` 同。
- **stdlib**：Mvc 先收回 Object；JSON keys/`get` 立刻 `objOf`（**0.55.22 已改 invokeObj**）。

**验收**：重建 runtime+monitor；`engine` 含 v0.55.21。**未提交**（你决定）。

### v0.55.20（2026-08-10）· remset 精简 + reflect 防脏指针

> **目标**：修 v0.55.19 崩溃：`hao_str_len` 且 rcx=ASCII「collectC」、`remset≈8007`；坚持 `while (i < s.length)` 不将就。

- **runtime**：晋升/过滤 remset **仅**仍握 young 子的 old；`expect_heap_*` **精确**用户指针；`field_get`/`str_len` 脏指针 panic。
- **IR**：while 每轮卸上一层条件 sticky（只留 hoist），再求值+pin。
- **stdlib**：撤回 JSON `.length` 缓存。

**验收**：重建 hao+runtime+monitor；`engine` 含 v0.55.20。**未提交**（你决定）。

### v0.55.19（2026-08-10）· while 条件 sticky + 崩溃栈

> **目标**：条件根跨迭代持有；崩溃日志可定位（寄存器/AV/堆栈 RVA）。

- **IR**：while：hoist/条件 spill `pin`；**先 genExpr 再 safepoint**；`clearLoopSpillSlots` 尊重 sticky 且不 pop。for 条件 safepoint 后置。
- **runtime**：`hao-crash.log` 写 access/rcx/rip/gpr + `CaptureStackBackTrace` RVA。
- **stdlib**：JSON 多处 `.length` 缓存（**0.55.20 已撤回**）。

**验收**：重建 hao+runtime+monitor；`engine` 含 v0.55.19。**未提交**（你决定）。

### v0.55.18（2026-08-10）· minor 扫 old 子图 + 晋升 remset

> **目标**：修 v0.55.17 仍同 rva `hao_str_len` UAF（~3min；engine 已确认）。

- **runtime**：minor 遇 old 不再裸 return，立即 `scan_precise` 入灰 young 子；minor 后晋升对象恢复 `remset_add`（major 仍禁 seed）；park 始终扫有界 C 叶。
- **stdlib**：`JSON.quote` 缓存 `s.length`。
- **坑债**：CF 续；纠正 BW「删晋升挂表」过激。

**验收**：GC smokes；`hao version`=0.55.18。**未提交**（你决定）。

### v0.55.17（2026-08-10）· STW 始终扫 GPR spill

> **目标**：修 v0.55.16 压测约 40min 后仍 `hao_str_len` UAF。

- **runtime**：`gc_scan_parked_thread` 始终扫 `gpr_spill`（safepoint 已溅射）；深 C 叶策略不变。
- **坑债**：CF 续。

**验收**：GC smokes；`hao version`=0.55.17。**未提交**（你决定）。

### v0.55.16（2026-08-09）· concat 新串根 / remove_root 禁 coop

> **目标**：修 v0.55.15 仍 `hao_str_len` UAF（rva 读 `(%rcx)`）。

- **runtime**：`hao_str_concat` 等先挂新串再摘旧；`remove_root` 不再协作 safepoint。
- **IR**：`+`/`+=` 拼接结果与 `.length` base `rootGcOperand`。
- **坑债**：CF 续。

**验收**：GC smokes；重建 monitor；`hao version`=0.55.16。**未提交**（你决定）。

### v0.55.15（2026-08-09）· str_len UAF：sweep 持 STW + 屏障 pin

> **目标**：修 v0.55.14 压测仍 `0xC0000005`（rva 落在 `hao_str_len` 读字节）。

- **runtime**：屏障/shade 协作窗口 pin old/new；sweep **不再先 leave、不中途放锁**；终止二次 seed。
- **坑债**：CF 续。

**验收**：GC smokes；`hao version`=0.55.15。**未提交**（你决定）。

### v0.55.14（2026-08-09）· add_root 协作锁 UAF：C 形参 scan_pin

> **目标**：修 v0.55.13 压测 `FATAL 0xC0000005`（漏标 C 形参）。

- **runtime**：`g_scan_pin` + 扫 parked 时始终 mark pin；`add_root*` 协作前钉住；崩溃日志输出 `rva`/`base`。
- **坑债**：CF 续。

**验收**：GC smokes；`hao version`=0.55.14。**未提交**（你决定）。

### v0.55.13（2026-08-09）· monitor HTTP+预热同停：终止握手 + root 协作锁

> **目标**：修 v0.55.12 后仍出现的「~40s 请求全 pending + 造垃圾打印停、进程不退」。

- **runtime**：终止 STW **禁止长 drain**（握手：seed/判空 → leave → 并发 drain）；`add_root*`/`remove_root` 用 `gc_lock_coop`；park 增加自旋 watchdog。
- **坑债**：CF 续。

**验收**：GC smokes；`hao version`=0.55.13。**未提交**（你决定）。

### v0.55.12（2026-08-09）· monitor 突然 pending：term drain 放锁 + 池 disarm

> **目标**：修「进程不退、请求 ~100ms 后突然全 pending、`parkWd=0`」。

- **runtime**：终止路径 `gc_drain_worklist(0)` 周期性放锁 + `GC_TERM_DRAIN_MS`（未抽空外层 leave/retry）；池 worker `disarm` 前先放池锁。
- **坑债**：CF 续。

**验收**：`gc_reclaim`/`gc_concurrent_mark`/`gc_stw_warmup`；`hao version`=0.55.12。**未提交**（你决定）。

### v0.55.11（2026-08-09）· monitor 压测 Web 假死：drain 上限 + park watchdog

> **目标**：修 `/api/gc` 长压测后进程仍在、HTTP 永不返回（非 OOM）。

- **runtime**：并发 `gc_drain_worklist(1)` 步数/时间上限；`park_wait` 2s watchdog 强行 `stw_leave`（`parkWatchdogTrips`）；终止路径仍可靠 abort。
- **net MVC**：`/api/*` 默认短连接，避免 keep-alive 钉死 4 worker。
- **08-gc-monitor**：预热随 `markAbort`/`stwIncomplete` 退避休眠。
- **坑债**：CF。

**验收**：`gc_reclaim`/`gc_concurrent_mark`/`gc_stw_warmup`；`hao version`=0.55.11。**未提交**（你决定）。

### v0.55.10（2026-08-09）· ?. / ?? / String 皮带 · T→T? 装箱根 · channel if_heap

> **目标**：按可达性补表达式/C 缺口；修 continue×finally 清槽回归（对 0.55.5）。

- **IR**：`?.`/`?.()` base + phi 结果 `rootGcOperand`；`??` phi 后重挂；`formatCallArg` 仅当 `coerce` 产生新 IR（`hao_box_*`）时挂根（全量再 root 曾致 OOP AV）；函数值 env 先 spill 再写 fnptr；`?.method` 的 `nnBlock` 在 box 之后记录。
- **runtime**：`hao_gc_add_root_if_heap`（同锁、无 safepoint）；channel 载荷用之；`hao_str_trim`/`to_upper`/`to_lower`/`byte_slice`/`hao_make_args` 皮带；reflect Class 镜直接 `add_root`；`hao_regex_compile` pattern 皮带。
- **测试**：`gc_safe_nav_string_smoke`、`gc_box_nullable_arg_smoke`。
- **回归修复（CE）**：`continue`/`break` **禁止**在 `emitUnwind`(穿过 finally) 之前 `clearLoopSpillSlots`（会杀 catch 池槽）；while `condL` 入口补清；池槽禁 `noteBlockGcSlot`。

**验收**：上述 smoke；`suite.exe`×50；`test.sh --rebuild-all` **1011**；`hao version`=0.55.10。**未提交**（你决定）。

### v0.55.9（2026-08-09）· 表达式面根收口 · C 皮带禁 is_heap_ptr 前置

> **目标**：堵住 pkg.fn/super/字段赋值/数组字面量/`+` 求值顺序/Func env；runtime 皮带勿在挂根前 safepoint。

- **IR**：`+`/`==` 先 `rootGcOperand(lhs)` 再求右；字段赋值 recv；数组字面量元素；pkg.fn/super 实参；Func 字段 env；模板 acc；定长 fill 先于 `array_new`；捕获形参先挂 `.arg` 再 `object_new`；lambda/方法组 env 即时 spill；非循环 spill alloca 进 **entry**。
- **runtime**：`array_push` / `substring` / `reflect_invoke` 直接 `add_root`；`str_concat` 去掉挂根前 `is_heap_ptr`（其内 safepoint → 假死/AV）。
- **测试**：`gc_expr_surface_root_smoke`。

**验收**：套件 **1011**；expr/call/array smoke；`hao version`=0.55.9。

### v0.55.8（2026-08-09）· 调用/串接/下标根 · C 皮带 · 函数体块清槽

> **目标**：堵住自由函数/泛型/Func 实参、字符串 `+`、下标/`??`、getenv/concat 的 SSA 假死；函数·lambda 顶层局部块尾可清。

- **IR**：`rootGcOperand` 覆盖自由函数/泛型/Func 实参与 env；串接左右；下标 base；索引赋值数组；`??` 左右；haoroutine env；`when.res` 先 spill 再清。
- **函数/lambda 体**：`beginBlockGcScope`（参数仍到 `root_unwind`）。
- **runtime**：`hao_str_concat` / `hao_os_getenv` 对堆串成对 `add_root`/`remove_root`。
- **测试**：`gc_call_arg_root_smoke`。

**验收**：套件 **1011**；call/block/continue/spill smoke；`hao version`=0.55.8。

### v0.55.7（2026-08-09）· 块/if 尾清槽 · hoist 边界 · select 清 spill

> **目标**：帧内死局部尽早不可达；勿 `root_unwind`（曾 AV）。

- **块作用域**：`beginBlockGcScope` / `noteBlockGcSlot` / 结束 `store null`（`{ }`、if/when/try/select case）。
- **hoist**：停嵌套 while/for；停嵌套裸块与 if/when/try/select（块尾自管）。
- **select**：结束清 h/sendStr spill 槽。
- **测试**：`gc_block_scope_root_smoke`。

**验收**：套件 **1011**；block/continue/spill smoke；`hao version`=0.55.7。

### v0.55.6（2026-08-09）· continue/recycle 清槽 · 调用实参根

> **目标**：堵住 spill 池遗漏与方法实参 SSA 假死。

- **while continue/break**：离开前 `clearLoopSpillSlots`（for continue 走 recycle）。
- **for recycle**：回退游标前对 `[target, highWater)` `store null`。
- **池扩容**：`emitEntryAllocaPtr` 插入函数 entry（支配全部 use）。
- **`rootGcOperand`**：方法 recv/实参跨 safepoint 先 spill（空 ir 的静态 `ClassName.f` 跳过）。
- **aspread/asize/空数组**：返回前不清 arrSlot；空数组亦进 shadow。
- **测试**：`gc_loop_continue_spill_smoke`。

**验收**：套件 **1011**；上述 + spill/loop smoke；`hao version`=0.55.6。

### v0.55.5（2026-08-09）· 循环 spill 池 · 合成根可达性收口

> **目标**：while/for 提升之后，循环内 **when/try/select/new/for.seq** 等合成根仍每轮 `root_push` → 假活；按可达性一次做对。

- **IR**：嵌套 while/for **共用** spill 池；循环前预分配槽（支配全部 use）；`emitSpillGcRoot` / when / catch / for.seq·iter / 未提升 GC 局部走 `acquireLoopGcSlot`；每层 `scopeStack` 清槽（内层不得误杀外层）；for 用 sticky+recycle。
- **try 结束**：catch 绑定 + `unwindGcRoot` 置 null。
- **new**：GC 实参先 spill；构造结束**勿**清 `objSlot`（防 SSA 假死）；循环内由池清。
- **块尾**：当时未做普通 `{ }` 清槽；**v0.55.7** 已用 `store null`（勿 `root_unwind`）。
- **测试**：`gc_loop_spill_pool_smoke`；原有 loop/for/when/precise smoke。

**验收**：套件 **1011**；上述 smoke；`hao version`=0.55.5。

### v0.55.4（2026-08-09）· for-in 局部提升 · 作用域纪律收口

> **目标**：while 已提升后，for-in 体内 var 对齐；写清方法/lambda/块与可达性的关系。

- **IR**：`genForArray` / `genForIterable` 调用同一套 `hoistVarDeclsInLoopBody`；循环结束清 null。
- **测试**：`gc_for_var_root_smoke`。

**验收**：套件 **1011**；for/while smoke；`hao version`=0.55.4。

### v0.55.3（2026-08-09）· while 局部提升 · 修 shadow 假活

> **目标**：v0.55.2 后 `liveBytes`/`remset` 已正常，但 monitor `blockCount` 仍随 warmup 台阶涨——根因是 IR 精确根泄漏，不是 sweep。

- **根因**：`while { var junk = ... }` 每轮 `root_push` 新槽，无限循环从不 `root_unwind` → 历史 List 对 GC **真可达**。
- **修复**：`genWhile` 提升体内 varDecls（跳过 lambda 体）；每轮只 store；循环结束 GC 槽置 null。
- **测试**：`gc_loop_var_root_smoke`（同型 40×2000，collect 后块数 < 15000）。
- **文档**：契约 §4.3；坑债 **BX**。勿把「排期 concurrent sweep」写成假活借口。

**验收**：套件 **1011**；loop/remset/根 smoke；`hao version`=0.55.3。

### v0.55.2（2026-08-09）· remset 假活根治 · major 禁伪根

> **目标**：修 warmup 晋升后 major 仍把历史 junk 标活（`liveBytes≈heapBytes`）——根因是 remset 语义，不是「GC 不懂可达性」。

- **P0**：`gc_seed_roots_and_remset` — major **禁止** enqueue remset；仅 minor 对 remset 容器 `scan_precise`。
- **P1**：minor 后去掉对所有晋升对象的盲目 `gc_remset_add`（屏障记真实 old→young）。
- **观测**：`hao_gc_stats`/`GC.remsetCount()`；08-gc-monitor 面板一行。
- **测试**：`gc_remset_major_smoke`；原有 GC smoke 不回归。
- **文档**：契约 R2（remset 不是根）；坑债 **BW**。

**验收**：套件 **1011**；上述 smoke；`hao version`=0.55.2。用户须 rebuild runtime + 重启 monitor。

### v0.55.1（2026-08-09）· fs/os 皮带成对摘根 · 修 monitor 堆涨

> **目标**：修 08-gc-monitor `blockCount`/heap 随刷新只涨不跌。

- **根因**：v0.55.0 给 fs/os 加了 `hao_gc_add_root`，未 `remove_root`；每次 `os.readFile`（Html 模板）永久钉死 path/内容。
- **修复**：`runtime_fs.c` / `runtime_os.c` 全路径成对摘根（对齐 net）。
- **测试**：`gc_os_root_belt_smoke`、`gc_html_refresh_smoke`。

**验收**：套件 **1011**；上述 smoke；`hao version`=0.55.1。

### v0.55.0（2026-08-09）· IR 精确根完备 · 可达性闭环

> **目标**：shadow-only STW 下，跨 safepoint 的活 GC 指针必须进 shadow；修假死亡面；文档去掉「纯靠 -O2 保守栈」过时口径。

- **IR**：when.subj/res、catch 绑定、select recvStr/h/sendStr、unwind GC 根槽、`$new`/语言 `new`/aspread·asize 合成循环 spill；构造/字面量临时 spill 用完清 null（防函数级假活）。
- **Runtime**：fs/os 跨 `os_block` 持 GC 指针补 `hao_gc_add_root`（对齐 net）。
- **测试**：`gc_root_when_catch_smoke` / `gc_root_select_smoke`；原有七条 GC smoke + 套件。
- **文档**：契约 R9；§9.1 拆已交付/排期/架构选择；README/语法/-O2 叙事改为双轨。

**排期（非本批）**：concurrent sweep / span。

**验收**：套件 **1011**；negcheck **28/28**；九条 gc smoke；`hao version`=0.55.0。

### v0.54.1（2026-08-09）· 诚实双轨根 · 修 monitor UAF

> **目标**：纠正 v0.54「有 shadow 就不扫 C」过纠；08-gc-monitor 在 4000 预热下可长期存活。

- TLS `g_in_os_block`；os_block/arm → STW 加扫 GPR + 有界 C 叶；仅 Hao safepoint → shadow-only。
- net send/recv/connect/udp：os_block 跨度 `hao_gc_add_root`。
- 契约 R2 / 坑债 **BT** / 记忆文档口径统一。

**验收**：monitor ≥60s + `/api/gc`；finalizer×20；七条 smoke；`hao version`=0.54.1。

### v0.54.0（2026-08-09）· 混合屏障 + Hao 精确根 + mark worker

> **目标**：废除「三项暂不动」虚假天花板；一代交付混合屏障、shadow 精确根、GC 私有 mark worker。

- **混合屏障**：`hao_gc_barrier` load old；MARK 期 shade(old)+shade(new)；IR/C 槽地址。
- **精确根**：`hao_gc_root_push/unwind`；STW 扫 shadow。
- **mark worker**：2 条私有线程（不注册 mutator）；并发 drain 每 16 步放锁；`markWorkerSteps`。
- 契约重写；坑债 **BS**；冒烟 `gc_hybrid_*` / `gc_precise_*`。
- **已知过纠（0.54.1 修）**：曾「有 shadow 跳过 C 叶」导致 monitor UAF。

**未做（下一代排期）**：concurrent sweep / span。

**验收**：`hao version`=0.54.0；七条 gc smoke。

### v0.53.5（2026-08-09）· IR/GC 埋点加固

> **目标**：扫清审计发现的半截埋点（park / ABBA / finalizer 复活 / 静态 shade），不将就。

- STW：`pool_join`、fs/os I/O `os_block`；`hao_sync_lock` 自旋 safepoint。
- channel：`try_send` 先 root 再加锁。
- finalizer：回调后仍可达则挂回堆；`test/gc_finalizer_resurrect_smoke.hao`。
- IR：Class/枚举 `emitGlobalGcStore`；aspread/asize + staticinit/`$new*` 入口 SP。
- 契约/坑债 **BR** / 记忆文档同步。

**验收**：`hao version`=0.53.5；五条 gc smoke。

### v0.53.4（2026-08-09）· IR↔GC 契约文档收口 + join park

> **目标**：把埋点/流程/规则写成唯一权威详文；补齐 `join` 对 STW 可见。

- 新建 [`docs/IR与GC契约.md`](../IR与GC契约.md)（R1–R8、流程图、IR/C 埋点、指标、检查清单）。
- 记忆文档 §5.4 / 文档治理改为摘要+链接；禁止多处复制契约。
- `hao_thread_join`：`hao_gc_os_block_enter/leave`。

**验收**：`hao version`=0.53.4；gc smokes。

### v0.53.3（2026-08-09）· 入口 safepoint + markAbort 可观测

> **目标**：加密协作 STW；abort MARK 可观测；finalizer 回调窗口挂根。

- IR：函数体 / lambda 入口 `hao_gc_safepoint`。
- API：`markAbortCycles` 进 `GcStats` / 面板。
- finalizer：回调期间临时 `hao_gc_add_root`。

**验收**：`hao version`=0.53.3。

### v0.53.2（2026-08-09）· 扩容 shade 原子化 + 静态字段 shade

> **目标**：堵住「黑数组 memcpy 后可打断 shade」正确性洞；静态 GC store 对齐 Dijkstra。

- **`hao_gc_array_copy_and_shade`**：持锁 memcpy + 补 shade，中间无 safepoint。
- **IR**：`emitGlobalGcStore` → `hao_gc_shade` 再写静态/全局 GC 指针。

**后续清单**：栈 map；混合屏障；入口 safepoint；mark worker。

**验收**：`hao version`=0.53.2。

### v0.53.1（2026-08-09）· IR↔GC 契约：数组扩容 shade + remset

> **目标**：补齐 C 侧与 IR 不一致的写屏障；修 List 扩容黑父白子；major 后清 remset，堆可回落。

- **`hao_array_push`**：先 barrier 再 store；扩容 `memcpy` 后对 ptr 元素 `hao_gc_shade`。
- **reflect / `hao_make_args`**：同样先 shade。
- **remset**：major 成功整表清空；`GC_PROMOTE_AGE` 2→4。
- **测试**：`test/gc_array_barrier_smoke.hao`；`08-gc-monitor` 堆振荡回落。

**验收**：冒烟绿；`hao version`=0.53.1。

### v0.53.0（2026-08-09）· 可达性闭环：对标 Go 的「不可达必回收」

> **目标**：修「标记轮次无法干净结束 → 白色 sweep 不到」；行为契约对齐 Go（终止可决断，禁止无限 MARK 黑囤）。

- **终止**：每轮 `gc_scanned_tid_clear`；本轮 soft STW 未齐 → 不得 `term_ok`。
- **失败 abort**：`gc_abort_mark_cycle` → IDLE + 丢 worklist + bump epoch + pacing；下一轮从根重来。
- **屏障**：IR 先 `hao_gc_barrier` 再 store；shade 前不 safepoint。
- **观测**：`hao_gc_stats` / `gc.GC.stats()` 单次快照；面板 `/api/gc` 改用。
- **测试**：`gc_reclaim_smoke` / `gc_concurrent_mark_smoke` / `gc_stw_warmup_smoke`；`08-gc-monitor` 实机。

**未做**：Yuasa/混合屏障；栈精确 map；独立 mark 线程。

**验收**：冒烟绿；`hao version`=0.53.0。

### v0.52.1（2026-08-09）· 页索引 + 池/channel park：回收真正发生

> **目标**：修 v0.52 面板仍「堆涨、collect=0、/api/gc 数百 ms、stwIncomplete 狂加」——根因在 find_block 与空闲 worker 对 STW 不可见，不是再把预热改回 40。

- **页索引**：`gc_find_block` O(页内块)。
- **池/channel**：`hao_gc_os_block_arm/disarm` 包 `cond_wait`（禁止持池锁 safepoint park）。
- **协作**：calloc 不持 GC 锁；指标入口 `gc_lock_coop`；STW 预算缩短；sweep 在 STW 结束后穿插放锁。
- **测试**：`gc_reclaim_smoke` / `gc_concurrent_mark_smoke` / `gc_stw_warmup_smoke`；`08-gc-monitor` 实机。

**验收**：`hao version`=0.52.1；4000 预热下 `collectCount`/`minor`/`major` 上升。

### v0.52.0（2026-08-09）· 软 STW + mark assist：修永不回收

> **目标**：修面板「incomplete 后从不 mark/sweep、堆只涨」；对齐 Go「先开屏障、并发推进、短终止」。

- **色纪元**：`gc_mark_epoch` O(1) setup，禁止持锁清百万 `marked`。
- **软根 STW**：未齐 park 仍扫已 park、进入 MARK；禁止整轮放弃。
- **Mark assist**：分配路径推进 grey；终止 STW 可重试，未齐保持 MARK。
- **API**：`gc.GC.heapBytes()` / `markAssistSteps()`；面板优先展示 heapBytes。
- **测试**：`test/gc_reclaim_smoke.hao`；套件 **1010**。

**未做**：Yuasa/混合屏障；并发 sweep；逃逸分析。

**验收**：冒烟绿；negcheck **28/28**；`hao version`=0.52.0。

### v0.51.0（2026-08-08）· 并发标记 + Dijkstra 写屏障

> **目标**：对标 Go 方向的并发 mark——短 STW 根/终止，标记与 mutator 重叠；不是全 STW 改名。

- **相位**：`IDLE → 根 STW → MARK（并发 drain）→ 终止 STW → sweep`。
- **Dijkstra 插入屏障**：`hao_gc_barrier` 在 `GC_PHASE_MARK` 时 shade 新指针；保留分代 remset。
- **黑分配**：标记期新对象 `marked=1`。
- **API**：`gc.GC.concurrentMarkCycles()`。
- **测试**：`test/gc_concurrent_mark_smoke.hao`；套件 `gcapi` +1。

**未做**：Yuasa/混合屏障；并发 sweep；独立 mark worker。

**验收**：冒烟绿；套件 **1008**；negcheck **28/28**；`hao version`=0.51.0。

### v0.50.4（2026-08-08）· GC STW 硬化：废除全标活 + 4000 预热可响应

> **目标**：修 `08-gc-monitor` 4000 预热导致 `/api/gc` 假死；不对齐「改小循环」将就；真正并发标记另批。

- **废除 mark-all**：STW 时间上限等齐 park；未齐 → `stwIncomplete`、本轮不 sweep、nursery/threshold pacing（有上限）。
- **阻塞路径**：`send`/`connect`/`sendto`/`recvfrom` 包 `hao_gc_os_block_*`（Win + POSIX）。
- **API**：`gc.GC.stwIncomplete()`；`stwMarkAllFallbacks()` 恒 0。
- **示例**：预热 `while (i < 4000)` + `serveBossWorkers(4)`；面板展示 incomplete。
- **测试**：`test/gc_stw_warmup_smoke.hao`；套件 `gcapi` 增两行。

**未做（下批）**：完整并发标记 / 增量写屏障（对标 Go）。

**验收**：冒烟三行 true；negcheck **28/28**；`hao version`=0.50.4。

### v0.50.3（2026-08-08）· Netty OIO：ByteBuf + Pipeline + boss/worker

> **目标**：根治 keep-alive 粘包；交付可测的 Netty 风格 OIO 栈（不做空壳改名；NIO poll 另批）。

- **`net.ByteBuf`**：字节累积 / readerIndex / discardReadBytes。
- **`Http.readOneMessage(sock, buf, …)`**：按完整消息边界切片，余量留缓冲；`completeMessageLen` / `chunkedBodyEnd`。
- **`Channel` / `ChannelPipeline` / `ChannelHandler` / `EventLoopGroup`**：serve 循环 `fireChannelRead(HttpRequest)` → `HttpResponse`。
- **`HttpServer.serveBossWorkers` / `HttpApp.serveBossWorkers`**：boss 只 accept，worker 池处理连接。
- **测试**：流水线粘包双响应；boss/worker 双客户端；套件 **1005**。

**未做（下批）**：非阻塞 + poll/WSAPoll EventLoop（NIO）。

**验收**：套件 **1005**；negcheck **28/28**；`hao version`=0.50.3。

### v0.50.2（2026-08-08）· 每类型 static Class 常量

> **目标**：`Foo.Class` 对齐「类型上的静态 Class 常量」（Java 类字面量），不再 IR 特判成员名。

- 编译器为每个有虚表的类合成 `static Class: reflect.Class`；`ensureInit` → `classOfMeta`。
- `Foo.Class` 走普通静态字段加载；删除 `IRGenExpr` 对 `"Class"` 的特判。
- **不是** Object 上共用一个 static（否则身份错）；每类型一份。
- `@interface` 补发射静态全局 + ensureInit。

**验收**：套件 **1000**；`hao version`=0.50.2。

### v0.50.1（2026-08-08）· Class 单例 + indexOf(from) + 清别名

> **目标**：废掉 Class `==` meta 特判将就；补齐 Java 式 `indexOf(from)`；删除兼容别名。

- **Class 单例**：`HaoClassMeta.classMirror`（meta 改 `global`）；`reflect.classOfMeta`；`TypeName.Class`/`typeOf`/`typeAt`/`classForName` 同对象；`==` 指针身份；equals 仍比 meta。
- **坑**：`meta: Long?` 存裸指针，勿对它做 `!= null` 智能转换（会按装箱 Long unbox）。
- **`String.indexOf(sub, from)`**：`hao_str_index_of_from`；Html 模板改用；删 `indexOfFrom`。
- **清别名**：删 `hasAnnotationEnding` / `HttpApp.annoEnds` / `acceptInto`。

**验收**：套件 **1000**；negcheck **28/28**；`hao version`=0.50.1。

### v0.50.0（2026-08-08）· 实例重载 + Class 身份 + HTTP keep-alive P1

> **目标**：实例方法按签名重载；Java 风格 `Class` 令牌；HTTP 连接会话 keep-alive（Netty 路线 Phase1）。

- **实例重载**：`ClassInfo` 允许多同名方法；IR 名加 overload 后缀；虚表槽键含签名；`ServerSocket.accept()` / `accept(Socket)` 共存。
- **`reflect.Class`**：原 `Type` 更名；`TypeName.Class` + `Object.getClass()`；当时 `==`/equals 比 meta（**v0.50.1** 升为单例）；注解 `HaoAnnoMeta` 增 classMeta；`isAnnotationPresent(Anno.Class)`。
- **MVC**：`register`/`scan` 主路径改 Class 令牌（`Controller.Class` / `GetMapping.Class`…）。
- **HTTP P1**：`serveConnection` 循环读→派发→写；默认 keep-alive；`Connection: close` 结束；`writeResponse(..., keepAlive)`。

**验收**：套件 **993**；`hao version`=0.50.0。

**后续**：v0.50.3 已交付 ByteBuf / Pipeline / boss-worker（OIO）；**仍未做** NIO poll EventLoop。
