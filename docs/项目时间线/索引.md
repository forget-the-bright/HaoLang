# 项目时间线 · 索引

> 各版本「做了什么」的权威正文在下列合订文档；本页仅索引表 + 链接。
> 踩坑与将就债见 [`../坑债.md`](../坑债.md)。规则与下一批见 [`../../记忆文档.md`](../../记忆文档.md)。

## 合订文档

| 文件 | 覆盖版本 |
|------|----------|
| [v0.05-0.09.md](v0.05-0.09.md) | v0.5 ～ v0.9 |
| [v0.10-0.19.md](v0.10-0.19.md) | v0.10 ～ v0.19 |
| [v0.20-0.29.md](v0.20-0.29.md) | v0.20 ～ v0.29（含 0.21.x 等补丁） |
| [v0.30-0.39.md](v0.30-0.39.md) | v0.30 ～ v0.39 |
| [v0.40-0.49.md](v0.40-0.49.md) | v0.40 ～ v0.49 |
| [v0.50-0.59.md](v0.50-0.59.md) | v0.50 ～ 当前 |

## 一句话索引（从旧到新）


> 下表为一句话索引；详细专节在对应合订文档（最新版在合订文首）。

| 版本 | 里程碑 | 一句话 |
|------|--------|--------|
| v0.5.0 | 结构化重构 | 单文件 `runtime.c`/`IRGen.cpp` 拆成多模块，为后续扩展打底 |
| v0.6.0 | 多文件 / 包 / import | Go 风格"目录即包"，跨包函数/类/接口/继承、private |
| v0.7.0 | 函数式集合 | 泛型函数从 `(T)->R` 递归推断嵌套类型参数；`collections.List<T>`；map/filter/forEach |
| v0.8.0 | extern C 机制 + 统一异常基类 | 标准库可调运行时 C；`exception.Exception`（纯 .hao） |
| v0.9.0 | 重载 + fmt 库化 + 泛型方法 + Map/Set/Queue | 函数重载、fmt 迁成 .hao 包、`List.map<R>`、开放寻址集合 |
| v0.10.0 | 外部 C 库 / 系统库导入 | 命令行 `-l/-L/--link` + 源码内 `@link`；链接 ws2_32/libm/手写 C |
| v0.11.0 | 外部库按需自动引入 | 默认 `-L` 搜索路径 + `@link` 支持路径 + 环境变量 `HAO_LDFLAGS/CFLAGS` |
| v0.12.0 | 系统库 os | 文件读写 / 环境变量 / 进程，跨平台 C 标准库 |
| v0.13.0 | 并发原语 sync | Mutex 自旋锁 + 原子整数操作，`__atomic` 内建跨平台 |
| v0.14.0 | 网络库 net | TCP/UDP socket，跨平台（Winsock/BSD） |
| v0.15.0 | 集合库扩充 | Stack<T>、for...in 迭代协议（toArray）、对象 key 的 Hashable 协议 |
| v0.16.0 | Object 根父类 + key 重做 | 所有类默认继承 Object（hashCode/equals/toString）；集合 key 改用 Object 协议 |
| v0.17.0 | OOP 类体系补全 | static 成员/静态构造器、extends/implements、super 构造链、enum、Go 式 init() |
| v0.18.0 | 泛型接口 + 迭代协议重做 | `Iterable<T>`/`Iterator<T>` 单态化、泛型类实现泛型接口、for...in 走接口分派 |
| v0.19.0 | 委托/注解/反射/时间/线程 + main args | delegate、annotation、reflect、time、thread、`func main(args)`、main 返回类型 bug 修复 |
| v0.20.0 | 注解 @interface + Action/Func + 反射 invoke + GC v2 | 注解改 Java 风格 `@interface`、预置 `Action`/`Func`、反射 invoke、GC v2 STW |
| v0.21.0 | **runtime 系统库动态加载（零 SDK）** | `hao_dl_*` + net Windows `LoadLibrary(ws2_32.dll)`；无 pragma/导入库 |
| v0.21.1 | **5.12 补齐 + Win CRT 最小集架构** | gc/thread/time/print 去 `windows.h` 导入；PEB 引导 dynload；`lib/sysroot/win-*` CRT |
| v0.22.0 | **基础类型包装 lang（装箱拆箱文档 + API）** | Integer/Boolean/lang.String/Double；隐式预导入；String 实例方法 |
| v0.23.0 | **位运算 + lang/os 整理 + pkg.Class** | `&\|^<<>>`；`lang.Bit`；os←path/io；修 `math.Math` |
| v0.24.0 | **语言级 Byte + 紧凑 [Byte]** | `TypeKind::Byte`/`u8`（LLVM 底层 i8）；数组头 `esz`；拒绝 Int 别名 |
| v0.25.0 | **数值体系对齐 Java/Kotlin** | Int=i32/Long=i64/Float=f32/Short；全包装；esz 1/2/4/8 |
| v0.25.1 | **v0.25 审计补齐** | Bit→Long；fmt 全族+Object；Short?/Byte? SSA；推断提升 |
| v0.26.0 | **内置类型全套** | 有符号/无符号成对+UIntPtr；`~`/移位掩码；静态重载；Bit Int/Long |
| v0.26.1 | **Bool ABI + 审计** | Bool→i8；`[Bool]` esz=1；Bit.getBit→Bool；无符号 toStr；MIN_VALUE 对齐 |
| v0.27.0 | **Char + String 堆头 + 字面量** | Char=i32；HaoString；`@"…"`/`@$`/`$@`；Length=码点 |
| v0.28.0 | **Regex + JSON + IO/HTTP** | Pattern/Matcher；Fastjson2 风 JSON；FileStream；Http |
| v0.47.0 | **包管理 HTTP A3** | WinHTTP zip + TOKEN/PROXY；depshttp；基线 954 |
| v0.47.1 | **仓目录规范** | `repo/LocalRepo` + `repo/RegisterRepo`；默认本地仓；基线 954 |
| v0.49.4 | **Socket 读契约** | EOF=空串 / null=错；recvExact；accept()；基线 984 |
| v0.49.3 | **HTTP/STW** | 阻塞 syscall parked；serveParallel；接口延迟 |
| v0.49.2 | **lambda 类型名** | 闭包内 Integer/String 静态调用不误捕；基线 981 |
| v0.49.1 | **进程资源监视** | os.Process + GC minor/major；08-gc-monitor 对标 .NET |
| v0.49.0 | **Web 补全** | Html 模板；Bean/Map/List→JSON；coerce；staticFiles；基线 972 |
| v0.48.0 | **HAO_REPO 本地仓** | Maven 模型；`module/version` 布局；源/本地拆分；基线 954 |
| v0.46.0 | **包管理传递依赖 A2** | BFS/exclude/冲突/why；depgraph；基线 954 |
| v0.45.0 | **包管理 semver A1** | `^`/`~`/`>=` 本地仓选最高版；depsemver；基线 954 |
| v0.44.0 | **hao fmt 缩进增强** | 4 空格花括号缩进 + 目录递归；基线 954 |
| v0.43.0 | **审计修复计划 B** | regex 独立捕获/HTTP 头安全+chunked/CHM 分段锁/GC 多轮 STW；基线 954；neg 28 |
| v0.42.1 | **回归审计修复** | JSON 转义/HTTP 字节分隔/64 混算拒绝/channel·net 并发/CHM·regex·reflect；基线 936；neg 28 |
| v0.42.0 | **hao test 正经协议** | `*_test.hao`+`TestXxx`+`testing`；废跑 main；基线 927；清债 H |
| v0.41.0 | **channel select** | select case/default；trySend/tryRecv；基线 927；neg 24 |
| v0.40.0 | **hao test / hao fmt** | 编译运行测试；空白规范化 -w/--check；基线 924 |
| v0.39.0 | **包管理第 1 层** | tidy/cache/lock/file 仓；`target/test/` 规范；基线 924 |
| v0.38.0 | **haoproject.json 第 0 层** | `hao mod init`；`localReferences`；不用 hao.mod；基线 924 |
| v0.37.1 | **回归审计 · channel** | `recvStr():String?`；句柄盒 finalizer；基线 924 |
| v0.37.0 | **多线程自动 GC** | 协作 safepoint；删门闩；放锁等 park；基线 921 |
| v0.36.0 | **haoroutine + channel + GC 加固** | 关键字启线程；channel 包；finalizer 锁外；内指针 mark |
| v0.35.1 | **GC v3 审计修复** | remset 跨 minor；静态根槽；by-ref/反射屏障；Win STW GPR |
| v0.35.0 | **GC v3** | 精确堆 + 分代 + 写屏障 |
| v0.34.2 | **回归审计（IR/runtime/stdlib）** | `[T]?` ABI；when 推断；异型==；throw 可空；池 UAF；GC 锁；CL 大小写；恢复线程/GC 测 |
| v0.34.1 | **`!!` 赋值 + 数组形参 by-ref** | 左值 `!!`；数组槽传参；extern 仍传数据指针；中途 GC 安全 |
| v0.34.0 | **集合完善 + gc API + 注释规范** | LinkedList/LHM/树/RB/TreeMap/CHM；`gc.GC`；5.14 注释规范 |
| v0.33.0 | **将就债 A～E 清零** | `$new` 字段默认；Map 满表抛错；原生 finalizer；`internal`；smart cast |
| v0.32.0 | **数组定长/展开 + 可扩展包扫描** | `new [T](n)`/`...`；`findTypes*`/`scanWhere*`；数组头 32B 修 GC |
| v0.31.0 | **多参构造反射 + MVC 注入** | `$new`/`newInstanceArgs`；`registerNewArgs`；修 ensureInit |
| v0.30.0 | **嵌套包扫描 + Web demo** | `HttpApp.scan`；reflect 类型表/`$new0`；demo.web(.api) |
| v0.29.0 | **Map 守卫 + reflect/注解 + net MVC** | Map 扩容溢出；invokeFloat/ptrOf/注解元数据；HttpApp + 注解路由 |
| v0.55.47 | **定位发现十三期** | sticky/block TRACE；catch dbg.value；haoroutine/when×for try 根冒烟 |
| v0.55.46 | **定位发现十二期** | pin/enter TRACE；for 迭代 dbg.value；ctor/静态方法 try 根冒烟 |
| v0.55.45 | **定位发现十一期** | 字段复合 dbg.value；unpin/leave TRACE；C 叶毒针 VERIFY；catch-only 根冒烟 |
| v0.55.44 | **定位发现十期** | 下标 dbg.value；GPR VERIFY；recycle TRACE；方法 try 根冒烟 |
| v0.55.43 | **定位发现九期** | refl_i64 VERIFY；复合/字段 dbg.value；acquire TRACE |
| v0.55.42 | **定位发现八期** | Unit-lambda ret；remset VERIFY；赋值 dbg.value |
| v0.55.41 | **定位发现七期** | 线程×try；再抛多层；pin VERIFY；薄 dbg.value |
| v0.55.40 | **定位发现六期** | break/lambda×try 根；VERIFY shadow_i；清槽 TRACE |
| v0.55.39 | **定位发现五期** | 控制面 try 根；清槽 STRICT；VERIFY 重入计数；select !dbg |
| v0.55.38 | **定位发现四期** | 嵌套 try 根；alloc VERIFY；catch/for declare + Except !dbg |
| v0.55.37 | **定位加深三期** | try×while×finally 根；VERIFY 坏根 loc；薄 dbg.declare |
| v0.55.36 | **定位门禁** | test.sh 挂 loc/spill；VERIFY；dbg 栈 TLS；try 根冒烟 |
| v0.55.35 | **定位发现** | loc_smoke/spill_ir；src=钉调用点；`[lib]` |
| v0.55.34 | **嵌套 sticky** | 内层 while 勿剥外层；block smoke 多轮绿 |
| v0.55.33 | **定位加深** | P0～P3；stack=；套件双门禁；G1 口径 |

| v0.55.32 | **定位底座 + 剩余收口** | L0 Diagnostic/`src=`；R4L/R5/R3/D1/DOC |
| v0.55.31 | **dbg 方法论补齐** | 按函数 DISubprogram；方法/lambda loc；hao命令 -g |
| v0.55.30 | **I0/I3+薄I4** | SourceLoc；`!dbg`；CLI `-g`；套件 1011 |
| v0.55.29 | **IROps 扩+I5 收口** | cast/gep/…；语法层可封装路径 0；口径钉死；套件 1011 |
| v0.55.28 | **I5 补遗+批5** | Except/Lit 补完；Class/Lambda；套件 1011 |
| v0.55.27 | **IROps + I5 批4** | 指令层收口；Literal/Except；套件 1011 |
| v0.55.26 | **I5 批3 Expr** | em_.emit 186→83；套件 1011 |
| v0.55.25 | **I1b + I5 批1/2** | Value+Control 迁封装；套件 1011 |
| v0.55.24 | **定位优先 Phase A** | Trace/fatal GC 快照/`emitSafepoint`；套件 1011 |
| v0.55.23 | **JSON/Http 回归** | Map 键 String?；contentLengthOf 独立帧；套件 1011 |
| v0.55.22 | **invokeObj≈Java** | Method.invoke→Object；Long 禁藏引用；Mvc/JSON/Html；CF 续 |
| v0.55.21 | **invoke i64 钉住** | refl_i64_pin；Mvc/JSON 立刻 objOf；CF 续 |
| v0.55.20 | **remset/reflect** | remset 仅 old→young；while sticky 不叠层；精确指针校验；CF 续 |
| v0.55.19 | **while 条件 sticky** | cond 后 safepoint；clear 尊 sticky；崩溃栈；CF 续 |
| v0.55.18 | **minor/晋升漏标** | old 子图扫 + 晋升 remset；修同 rva str_len；CF 续 |
| v0.55.17 | **STW 扫 GPR** | park 始终扫 gpr_spill；修 40min str_len UAF；CF 续 |
| v0.55.16 | **concat/str_len UAF** | 新串先挂根；remove 禁 coop；IR +=/length；CF 续 |
| v0.55.15 | **str_len UAF** | sweep 持 STW；屏障 pin；二次 seed；CF 续 |
| v0.55.14 | **add_root 协作 UAF** | `g_scan_pin`；崩溃日志 rva；CF 续 |
| v0.55.13 | **HTTP+预热同停** | 终止握手；root 协作锁；park 自旋 watchdog；CF 续 |
| v0.55.12 | **monitor 突然 pending** | term drain 放锁；池 disarm 不持锁；CF 续 |
| v0.55.11 | **monitor 压测 Web 假死** | 并发 drain 上限；park watchdog；`/api` 短连接；CF |
| v0.55.10 | **?. /装箱根 + String/channel 皮带** | `add_root_if_heap`；CE：continue 勿在 finally 前清池槽 |
| v0.55.9 | **表达式面根 + 皮带纪律** | pkg/super/字段/字面量/`+`序；禁 `is_heap_ptr` 前置；`gc_expr_surface_root_smoke` |
| v0.55.8 | **调用/串接/下标根 + C 皮带** | 自由函数·泛型·Func；`str_concat`/`getenv`；函数体块清槽；`gc_call_arg_root_smoke` |
| v0.55.7 | **块尾清槽 + hoist 边界** | 块/if store null；select 清 spill；`gc_block_scope_root_smoke` |
| v0.55.6 | **continue/recycle + 方法调用根** | continue 清池；recycle 置 null；方法 `rootGcOperand`；`gc_loop_continue_spill_smoke` |
| v0.55.5 | **循环 spill 池** | when/try/new/for.seq 合成根复用槽；`gc_loop_spill_pool_smoke` |
| v0.55.4 | **for-in 局部提升** | 与 while 同纪律；作用域×可达性写清 |
| v0.55.3 | **while 局部提升 · shadow 假活** | 循环内 var 只 push 一次；`gc_loop_var_root_smoke` |
| v0.55.2 | **remset 假活根治** | major 禁 enqueue remset；晋升勿盲目挂表 |
| v0.55.1 | **fs/os 皮带成对摘根** | 修 Html/readFile 假活；`gc_os_root_belt_smoke` |
| v0.55.0 | **IR 精确根完备** | when/catch/select/unwind/new spill；契约 R9 |
| v0.54.1 | **诚实双轨根 · 修 monitor UAF** | os_block 必扫 C 叶；net `add_root`；契约 R2 |
| v0.54.0 | **混合屏障 + 精确根 + mark worker** | Yuasa+Dijkstra；shadow stack；`markWorkerSteps` |
| v0.53.5 | **IR/GC 埋点加固** | park 漏点；try_send ABBA；finalizer 复活；静态 shade |
| v0.53.4 | **IR↔GC 契约文档 + join park** | [`IR与GC契约.md`](../IR与GC契约.md)；`hao_thread_join` os_block |
| v0.53.3 | **入口 safepoint + markAbort** | 函数/lambda 入口 SP；`markAbortCycles`；finalizer 挂根 |
| v0.53.2 | **扩容 shade 原子化 + 静态 shade** | `hao_gc_array_copy_and_shade`；`emitGlobalGcStore` |
| v0.53.1 | **数组屏障 + remset 清** | push 先 barrier；major 清 remset；PROMOTE_AGE=4 |
| v0.53.0 | **可达性闭环 abort MARK** | 终止须齐；失败 abort；先 shade 再 store |
| v0.52.1 | **页索引 + 池/channel park** | find_block；arm/disarm；sweep 穿插放锁 |
| v0.52.0 | **软 STW + mark assist + 色纪元** | 先开屏障；assist；heapBytes |
| v0.51.0 | **并发标记 + Dijkstra** | 短 STW 根/终止；写屏障 shade |
| v0.50.3 | **Netty OIO：ByteBuf / Pipeline / boss-worker** | 粘包根治；ChannelPipeline；EventLoopGroup |
| v0.50.2 | **每类型 static Class 常量** | 合成 `static Class`；废 IR `.Class` 特判；注解亦 ensureInit |
| v0.50.1 | **Class 单例 + indexOf(from) + 清别名** | `classMirror`；废 `==` meta 特判；删 annoEnds/acceptInto/indexOfFrom |
| v0.50.0 | **实例重载 + Class 令牌 + HTTP keep-alive P1** | 签名重载；`TypeName.Class`；MVC meta 匹配；连接会话 keep-alive |


## 维护约定

1. **升版或本版专节**：写入对应十位合订文件（如 `0.36.0` → `v0.30-0.39.md` 文首插入专节），并更新本索引表一行。
2. **记忆文档 / README** 只保留当前版本摘要 + 指向本目录的链接，不复制整段专节。
3. 同大版本补丁（`0.21.0` / `0.21.1`）写在同一合订文件内相邻专节。
