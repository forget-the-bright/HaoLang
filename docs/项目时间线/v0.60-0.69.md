# 项目时间线 · v0.60.x ～

> 同十位大版本合订；细节坑见 [`../坑债.md`](../坑债.md)。
>
> 索引：[`索引.md`](索引.md)

### v0.69.0（2026-08-13）· GC-ALIGN-4：弱/软引用 + 语言 OOM + 速率 + 跨线程 VERIFY

> **目标**：关掉上批诚实 P2 中可交付切片；页级 mspan 当时标 ARCH 非目标（**v0.73 已推翻并交付**）。
> **改**：WeakRef/SoftRef 侧表；OutOfMemoryException+GC.boot；miss 粘滞 Hao 源码行；GcStats 速率槽；VERIFY 扫全线程 shadow；OOM 前清 soft 再试。
> **验收**：gc_p2_gate；全量 + negcheck；默认仍 **F 增量编译**。

### v0.68.0（2026-08-13）· GC-ALIGN-3：禁 FULL + BITMAP + finalizer SEH

> **目标**：消灭方法论红线「非保守堆」假勾；finalizer 异常将就；C 叶/SPAN 名同频。
> **改**：`GC_KIND_BITMAP` 尾部位图（>32 槽）；`hao_object_new_map`；`gc_alloc`→OPAQUE；VERIFY 禁活堆 FULL；复活可达对齐 shadow/pins；Win finalizer SEH；清单速率/野指针诚实化；`gc_bitmap_wide_gate`。
> **验收**：bitmap/VERIFY/freelist/doc/barrier 门禁；全量 + negcheck；默认仍 **F 增量编译**。

### v0.67.0（2026-08-13）· GC-ALIGN-2：清假债 + VERIFY freelist

> **目标**：文档假债清零；VERIFY 扫 freelist/doomed；导出 `freelistHits`。
> **改**：契约/清单/坑债/记忆同频（exact-size freelist；禁「余 span / argmem / 开放 GC-NEXT」）；`gc_verify_freelist_integrity`；`hao_gc_freelist_hits` / `GC.freelistHits()`；门禁 `gc_doc_stale_gate` / `gc_verify_freelist_gate` / `gc_freelist_hits_gate`。
> **验收**：三门禁 + `barrier_ir_gate`；全量 + negcheck；默认仍 **F 增量编译**。


### v0.66.0（2026-08-12）· GC-ALIGN：exact-size freelist + 契约对齐

> **目标**：纠正 v0.64 假 SPAN；GC 代码↔方法论不一致一口气修完。
> **改**：exact-size freelist（≤64KiB；`spanSweepChunks`=入队；**drain 在 trampoline 外**）；barrier IR `memory(readwrite)`；`HAO_GC_VERIFY` 堆 walk；OOM 带 heap/live/thr/blocks；清单/契约同频。
> **验收**：span/concurrent smoke；`barrier_ir_gate`；全量 + negcheck；默认 → **F 增量编译**。


### v0.65.1（2026-08-12）· VERIFY-NOCRT + TOOL-LSP0

> **目标**：VERIFY 零 snprintf；插件诊断→Problems；关总计划 A～F。
> **改**：`gc_verify_fatal_ip` 手写格式化；`verify_nocrt_gate`；插件 `diagnostics.ts` **0.1.4**；`lsp0_diag_gate`。
> **验收**：两门禁绿；全量 **1049**/0；negcheck 38/38；默认 → **F 增量编译**。

### v0.65.0（2026-08-12）· BARRIER-FAST

> **目标**：`hao_gc_barrier` IDLE 无锁/短锁快路径；关 BJ。
> **改**：IDLE+无 STW：`new_val==null` 无锁返回；否则短锁 remset。
> **验收**：barrier/hybrid smoke；全量随 0.65.1。

### v0.64.0（2026-08-12）· GC-NEXT-SPAN：分片 free（假结案；勿当现状）

> **目标（当时）**：concurrent free 可观测分片；误关 GC-NEXT。
> **改**：yield 片累计（**非** size-class freelist）；`gc_span_sweep_smoke`。
> **真相**：真 exact-size freelist 见 **v0.66.0**。
> **验收**：smoke 绿；全量 **1049**/0；negcheck 38/38；默认 → **BARRIER-FAST**。

### v0.63.0（2026-08-12）· G2-SYM：符号登记迁 Sema

> **目标**：IRGen 零 `syms_.declare*`；登记经 `hao::symDeclare*`（`src/sema/SymBind.*`）。
> **改**：全部 declare 调用点改走 SymBind；门禁 `sema_sym_gate`；坑债 G2 结案；方法论分层 `[x]`。
> **验收**：gate OK；`test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.63.0；默认 → **GC-NEXT-SPAN**。

### v0.62.1（2026-08-12）· DOC-STALE-4：假债清扫

> **目标**：清 FEAT-SELECT 后文档假债；落 A～F 关表；不插队代码债。
> **改**：记忆 §9 select 真等待；方法论 §3.6↔§十对齐；channel/契约措辞；坑债总计划表。
> **验收**：`test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.62.1；第 10 章默认 → **G2-SYM**。

### v0.62.0（2026-08-12）· FEAT-SELECT：select 真等待

> **目标**：关第 10 章 🔥1——`select` 去 sleep 忙等，对齐方法论多路 wait。
> **改**：
> - runtime：`hao_chan_select` + 每 chan select-waiter 链；send/recv/close 唤醒；`os_block` park
> - IRGen：`genSelect` 建 ops 表调 `@hao_chan_select`；有 default 返回 -1
> - 独立 [`test/select_wait_smoke.hao`](../../test/select_wait_smoke.hao)；hao语法 §15 / channel 头注释 / 坑债 AP 脚注
> - 调试方法论「分层清晰」改为首刀+符号 P2 口径
> **验收**：独立 smoke + 既有 select 冒烟绿；`test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.62.0；第 10 章默认 → **TOOL-LSP0**。

### v0.61.2（2026-08-12）· DOC-STALE-3：方法论假债三次清扫

> **目标**：关契约 Sweep 表漂移 + 调试/GC 清单假债 + 坑债历史误读；不插队 FEAT-SELECT。
> **改**：
> - 契约 §3 Sweep：默认 concurrent（先 leave）；`HAO_GC_STW_SWEEP=1` 才整段 STW
> - 调试方法论 IRGen/验收主路径 `[x]`；Statepoint **ARCH 已做等价**
> - 坑债 BJ/BQ 历史「停顿排期」标当时；现状指向 v0.61 首交付
> - GC 清单 §十四 NativeHandle **已做等价**；JNI 全套 **P2**
> **验收**：`test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.61.2；第 10 章默认仍 **FEAT-SELECT**。

### v0.61.1（2026-08-12）· DOC-STALE-2：方法论假债二次清扫

> **目标**：清 v0.61.0 后仍写「仍开」/「停顿排期」的假债面；加硬藏针门禁；不插队 FEAT-SELECT。
> **改**：
> - 契约 §4.3 历史段：去假「全量 Unwind/TRACE helper 仍开」；改「截至 v0.55.x；v0.61 已关」
> - README / 记忆：concurrent sweep 首交付；Handle 已切，删串上移「仍开」
> - GC 清单：主路径根/屏障/安全点/控制流 `[x]` + ARCH/P2 旁注
> - 坑债延期补 **G2**；契约 §8 挂 METH 永久门禁列表
> - `handle_resource_gate`：扩 Long 禁名 + 资源包 `h` + 底层槽须 NativeHandle
> **验收**：`handle_resource_gate` OK；`test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.61.1；第 10 章默认仍 **FEAT-SELECT**。

### v0.61.0（2026-08-12）· METH：方法论激进全修复 epic

> **目标**：关方法论审计缺口（T01～T10）；禁中途全量；全量只一次。
> **改**：
> - T01/T02：`ThreadPool`→`NativeHandle`+drop；`handle_resource_gate`
> - T03：IRGen `traceIrgen` 统一 helper；`trace_helper_gate`
> - T04：Unwind↔GC 全量关勾；`unwind_full_gate`；throw 后禁插 dbg loc
> - T05/T06：barrier IR `#1` noinline+memory；语句级 `stmtGcTemps`（嵌套 define 隔离 `emitFnDepth_`）
> - T07：concurrent sweep 首交付（摘链+放锁 free；`HAO_GC_STW_SWEEP=1` 可关；`concurrentSweepCycles`）
> - T08：G2 首刀 `src/sema/TypeResolve.*`；`sema_cut_gate`
> - T09：五份方法论 + 坑债/契约同频；方法论优先口径
> **验收**：独立测全绿后 `test.sh --rebuild-all` **1049**/0；negcheck **38**/38；`hao version`=0.61.0；第 10 章默认仍 **FEAT-SELECT**。

### v0.60.4（2026-08-12）· DOC-STW：软 STW 握手硬修 + 门禁结案

> **目标**：关 **SOFT-STW / 🔥1 DOC-STW**；禁 SuspendThread；禁 incomplete→0 当绿。
> **改**：
> - [`runtime_gc.c`](../../stdlib/runtime_gc.c)：跨轮保持 `stw_request`；热 miss 宽限 AGE 96ms / GRACE 24ms；根/终止预算 96/72ms、5 轮×16ms
> - 门禁：[`test/gc_stw_handshake_gate.hao`](../../test/gc_stw_handshake_gate.hao) + [`script/win/stw_handshake_gate.ps1`](../../script/win/stw_handshake_gate.ps1)（collect 进展 + 分相不变量；**不**要求 incomplete==0）
> - 复现分类：无 os_block 洞；残留为 Win 协作握手税（非 UAF）
> **验收**：`stw_handshake_gate` OK；`test.sh --rebuild-all` **1049**/0；`hao version`=0.60.4；SOFT-STW 从延期表移除；第 10 章默认 → **FEAT-SELECT**。

### DOC-only · 记忆/文档治理收口（2026-08-12，语言仍 0.60.3）

> **目标**：同频字段校正；第 10 章只留开放 🔥；将就主表清空假开项；默认任务 **DOC-STW**。
> **改**：[`记忆文档.md`](../../记忆文档.md)、[`文档治理.md`](../文档治理.md)、[`坑债.md`](../坑债.md) 第二节、README 抬头；工具轨叙事 **0.1.3**。
> **验收**：记忆无过期 1011/28/28；新会话路径 0→1→坑债→🔥1 可执行。

### v0.60.3（2026-08-12）· 强制 new 数组 + Array + T... + 自动属性

> **目标**：一版关坑交付 RFC-0005/0006/0007。
> **改**：
> - 废止裸 `[…]` 构造；`new [T]{…}` / `new [T]{}`；`[T] <: Array <: Object`；`length`/`capacity`/`clone`；`hao_array_clone`
> - `T...` 末位可变参数（隐式 new / 直传 / 空参）
> - 自动属性：`=` 默认值、自定义 get/set 体、accessor 可见性、`OnChange(fn)`；赋值走 setX
> - 门禁：`target/p14-smoke`～`p16-smoke`；stdlib/suite 字面量迁移

**验收**：p14/p15/p16 绿；批末 suite+negcheck；`hao version`=0.60.3。

### 工具轨 · vscode_plugin 0.1.3（2026-08-12，语言仍 0.60.3）

> **目标**：Phase 0+1；TextMate 全覆盖；`hao` CLI；拟人比好图标（无全局图标主题劫持）。
> **改**：[`tools/vscode_plugin`](../../tools/vscode_plugin)——高亮 / snippets / problemMatcher / fmt / 状态栏 / `hao-mascot` 图标；无独立 LSP/Debug。

**验收**：`npm run compile`；SYNTAX_COVERAGE；语言 icon 仅作用于 `.hao`。

### v0.60.2（2026-08-12）· List/Arrays API 补面

> **目标**：List `lastIndexOf`/`remove`/`addAll([T])`；`Arrays.copyOfLength(a, newLength)`。
> **改**：List/ArrayList/LinkedList；Arrays；`target/p13-smoke` + P13。

**验收**：p13 绿；套件 **1049**；`hao version`=0.60.2。

### v0.60.1（2026-08-12）· 具名实参 + RFC 体系

> **目标**：调用处 `name: expr` 语义落地；类型系统特性进正式 RFC。
> **改**：
> - [`docs/RFC/`](../RFC/)：模板 + 0001～0004（装箱/转型/通配结案；具名 Implemented）
> - IRGen：`bindCallArgs` / `collectCallArgsForFormals` / 方法·符号重载具名；全调用路径接线；Func 值拒具名
> - 门禁：`target/p12-smoke` + `p5_smoke` P12

**验收**：p12 正反绿；`hao version`=0.60.1（全量套件见同会话 0.60.2 批末）。

### v0.60.0（2026-08-12）· 数组业务上移结案

> **目标**：结案「数组业务在 Hao」；算法层用 Hao 组合内建 `[T]`；布局与带屏障 push/pop 写死永久 C。
> **改**：
> - [`Arrays.hao`](../../stdlib/src/collections/Arrays.hao)：`isEmpty`/`contains`/`indexOf`/`lastIndexOf`/`equals`/`copyOf`/`copyOfRange`/`fill`（相等用 `==`）
> - `List`：`contains`/`indexOf`/`clear`；`ArrayList`/`LinkedList` 实现
> - IRGen：`genPendingFunctionInstances` 恢复模板包 import（否则泛型函数体里 `exception.Exception` 在 main 实例化点解析失败）
> - 门禁：`target/p11-smoke` + `p5_smoke` P11；套件 +11 → **1043**
> - **不改** `runtime_array.c` / 写屏障路径；不删 `hao_array_*` 符号

**验收**：`p5_smoke` → `P5_SMOKE+IR_SYNC+P7+P8+P9+P10+P11 OK`；`test.sh` 1043；`negcheck` 34/34；`hao version`=0.60.0。
