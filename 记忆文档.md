# HaoLang 项目记忆文档

> **给接手的 AI / 开发者（新会话必读）**  
> 本文件保留**工作规则、设计决策、代码地图、下一批**；版本专节见 [`docs/项目时间线/`](docs/项目时间线/索引.md)，坑与将就债见 [`docs/坑债.md`](docs/坑债.md)。旧聊天**不可依赖**；按下方顺序读完即可开工。
>
> | 必读顺序 | 内容 |
> |----------|------|
> | **第 0 章** | 环境、接入清单、防回归、本批收工、**方法论速查** |
> | **第 1 章** | 硬性规则（**中文** / **文档同步** / **.bak** / 测试 / 不将就）——全文在此 |
> | **坑债 · 将就债 + 第 10 章** | [`docs/坑债.md`](docs/坑债.md) 第二节 + 本文件第 10 章下一批 |
> | 动手时再查 | [`docs/坑债.md`](docs/坑债.md) · 第 7 代码地图 · 第 5 设计 · 第 8 构建 · 第 3～4 特性/库 · [`docs/项目时间线/`](docs/项目时间线/索引.md) · 用户长文 [`docs/hao命令.md`](docs/hao命令.md) / [`docs/hao语法.md`](docs/hao语法.md) |
>
> **当前版本**：**0.55.49**｜套件基线 **1011** 行（双门禁+loc/spill）｜反向 `negcheck.ps1` **28/28**｜宿主 `win-amd64`｜可交叉 `linux-amd64`（musl）
> **GC 权威详文**：[`docs/IR与GC契约.md`](docs/IR与GC契约.md)（勿在聊天/多处复制）  
> **本版能力**：定位发现十五期——unwind/catchDepth TRACE；fn/this 薄 `dbg.value`；catch-finally-throw 根冒烟；ext 根毒针。  
> **阶段**：架构优化。下一批：功能轨 / 完整 Hao 类型视图（见第 10 章）。
> **方法论**：[`docs/方法论/`](docs/方法论/)——设计权威；示例函数名非强制。
>
> **四条铁律（用户反复强调，违反即不合格）**：
> 1. 对用户**全程中文**（回复、思考说明、提交说明、两份文档叙述；代码标识符可英文）。
> 2. **每一步做完**且测试绿 → **立刻**按 [`docs/文档治理.md`](docs/文档治理.md) 更新：本文件摘要 + README 用户可见面 + 时间线专节 + 坑债（完成项/踩坑）。禁止只改代码。
> 3. **不做将就**（规则 8/11）：别名凑合 / 半套类型 / 只测正向 / 静默吞错 → 不允许；正反向测试齐。
> 4. **改源码先 `.bak`，收工删备份**（规则 12）：大文件禁止无备份整文件覆盖（v0.32 曾冲掉 `IRGenLiteral.cpp`）。

---

## 文档怎么用（先看这里）

> 完整治理约定见 [`docs/文档治理.md`](docs/文档治理.md)。

| 文档 | 读者 | 写什么 | 不写什么 |
|------|------|--------|----------|
| **`记忆文档.md`（本文件）** | 接手 AI | **规则**、方法论、设计决策**摘要**、特性/库速查、代码地图、限制摘要、下一批、批末清单 | 完整版本专节、完整坑表、**整份 IR↔GC 契约**、冗长教程 |
| **`README.md`** | 用户 / 对外 | 介绍、快速开始、命令表、限制摘要、构建入口；详文链 `docs/` | 完整语法书、完整 stdlib API、IRGen、时间线全文 |
| **[`docs/IR与GC契约.md`](docs/IR与GC契约.md)** | 改 IRGen / GC | **埋点、流程、R 规则、指标、检查清单** | 版本流水账、语法教程 |
| **`docs/hao命令.md`** | 用户 | `hao` 子命令原理；包管理字段/环境变量；`hao test` 规则 | IRGen、版本流水账 |
| **`docs/hao语法.md`** | 用户 | 语法语义、限制、对标 Go/Java/C# | 命令行细节、完整 API 表 |
| **`docs/项目时间线/`** | 查历史 | 各版本做了什么（十位合订） | 规则、坑表、契约全文 |
| **`docs/坑债.md`** | 修 bug / 清债 | 踩坑表 + 将就债 | 教程、里程碑叙述、契约全文 |
| **`docs/方法论/`** | 架构优化 / 评审 | 语言分层与收口原则、GC 全覆盖清单、Debug 约束 | 当前实现流水账、版本专节 |

**同频**：版本号、基线行数、下一步、用户可见限制——改一处必核其它。

**章节地图**（新会话最少 **0 → 1 → 坑债·将就债 → 10**）：

| 章 | 内容 | 何时看 |
|----|------|--------|
| **0** | 速览 + 接入 + 防回归 + 收工摘要 + 方法论 | 每次新会话第一眼 |
| **1** | 工作规则（全文保留于此） | 改任何代码前 |
| **2** | 版本时间线（→ 合订文档） | 查某版怎么做的 |
| **3** | 已实现特性**速查表**（详文 → `hao语法`） | 「语言会不会 X」 |
| **4** | 标准库**速查表**（详文 → 源码注释 / `hao语法`） | 写 `.hao` / 加库 |
| **5** | 设计决策（5.12 dynload；5.13 分包；5.14 注释；**5.15 项目清单**） | 改 runtime / IRGen / 加包前 |
| **6** | 踩坑 / 将就债（→ `docs/坑债.md`） | 遇诡异 bug；批末必补 |
| **7** | 代码地图 | 动手改之前 |
| **8** | 构建 / 测试 / 打包 / negcheck | 验证与发版 |
| **9** | 已知限制摘要 + 将就债指针 | 别重复已排除项 |
| **10** | 接下来要做 | 开新批次的依据 |
| **附** | 批末检查清单 | 收工自检 |

---

## 第 0 章 · 项目速览与新会话接入

### 0.1 这是什么

**HaoLang（好语言）**：用 C++17 从零构建的现代静态类型编译型语言，对标 Go / Kotlin / Java，最终目标**自举**。

| 项 | 说明 |
|----|------|
| 源码 | `.hao` |
| 命令 | `hao`（对标 `go`：`run/build/emit/parse/tokens/mod/test/fmt/clean/env/version`） |
| 管线 | ANTLR4 → C++ IRGen 生成**文本 LLVM IR**（不链 LLVM C++ API）→ clang + lld + `libhaort.a` → 原生可执行文件 |
| 宿主 | `win-amd64`；用户程序可交叉到 `linux-amd64`（musl 静态） |
| 并发关键字 | **`haoroutine`**（禁止叫 goroutine） |
| 自举位置 | 计划 `hao-build-lang/` 包 |
| 发行包 | `script/win/package.ps1` → `target/win-amd64-haolang-<ver>/`；须含 `bin/` + `stdlib/src/` + `lib/llvm/` + `lib/sysroot/win-amd64/lib/` + `examples/`（←`haolang-example/`）+ `docs/`（hao语法/hao命令） |
| 用户示例 | **`haolang-example/`**（权威）；`test/` 仅套件/冒烟，非对外教程 |

### 0.2 新会话接入清单（照做即可开工）

1. **环境**：仓库根执行；**新机器**先 `script\setup_env.ps1`（scoop + `fetch_msvc` / `fetch_llvm` / `fetch_winlibs` / `fetch_sysroot`），再 `. .\env.ps1`。MSVC STL 来自本机 VS（`fetch_msvc` 可装 Build Tools；非 LLVM、非 MinGW）。代理：`script\win\proxy.local.ps1`（gitignore）或 `HAO_HTTP_PROXY`。已有完整 `lib/` 时勿重复强制下载。
2. **读约束**：第 1 章全文——尤其：
   - **规则 1**：对用户与文档**全程中文**；
   - **规则 2**：`改代码 → 测试绿 → 立刻按文档治理更新（本文件摘要 + README + 时间线 + 坑债）`；
   - **规则 12**：改源码前 `.bak`，收工删备份；
   - 规则 8/11 不将就；规则 10 零 SDK；加包读 **5.13**；stdlib 注释读 **5.14**。
3. **看任务**：[`docs/坑债.md`](docs/坑债.md) 将就债 + 第 10 章「🔥 下一批」——**未指定任务时：开干架构方法论比对与文档治理**（见 §10 表；Netty NIO 已后移）。
4. **改代码**：第 7 章找文件 → **先 bak** → 改；系统 API / 链接读 **5.12**；加包读 **5.13**；**public API 先写注释再测（5.14）**。
5. **验证**（按改动选跑）：
   ```powershell
   powershell -ExecutionPolicy Bypass -File script\win\build_runtime.ps1   # 改了 runtime_*.c
   cmake --build build --parallel 8                                   # 改了编译器 C++（Release）
   bash script/test.sh --rebuild-all                                  # 改 stdlib/语法/编译器后强制全量
   powershell -ExecutionPolicy Bypass -File script\win\negcheck.ps1       # 动可空/类型时
   ```
   验收：套件 stdout **≥ 1011** 行、退出码 **0**；动网络再跑 HTTP/套件 iohttp/mvc/webdemo/webext；交叉冒烟：`hao build test/suite --target linux-amd64 -o output/suite-linux` 魔数 `7f 45 4c 46`（交叉前须 `build_runtime.ps1 -Target linux-amd64`）。反向须 **28/28**。
6. **若动 Windows 链接 / 打包**：
   ```powershell
   . .\env.ps1; haobuild
   powershell -ExecutionPolicy Bypass -File script\win\build_runtime.ps1
   powershell -ExecutionPolicy Bypass -File script\win\fetch_winlibs.ps1 -Target win-amd64   # 必要时 -Force
   # 打包前：终端不要 cwd 停在 target\...\bin（会锁目录）
   powershell -ExecutionPolicy Bypass -File script\win\package.ps1 -Zip
   ```
7. **收工**：删本批全部 `.bak` → 按「附：批末检查清单」勾选 → 两份文档同频 → **中文**。

### 0.3 当前能力快照

> 与 **v0.55.10** 对齐；版本流水见 [`docs/项目时间线/`](docs/项目时间线/索引.md)，此处只保留能力面。

- 语言：`haoroutine` / `channel` / `select`；实例重载；协作 GC safepoint + shadow 精确根。
- 包管理：`HAO_REPO` + `HAO_REGISTRY`；`hao mod` / `hao test` / `hao fmt`。
- Web：`net.Html`、JSON coerce、`staticFiles`、`serveBossWorkers`；示例 `08-gc-monitor`。
- GC：可达性主路径已交付；细节见 §5.4 / [`docs/IR与GC契约.md`](docs/IR与GC契约.md)。
- 工具链：`script/setup_env.ps1` + `script/win/*`；注释规范 **5.14**。

### 0.4 防回归要点（新会话必看）

| 点 | 要点 |
|----|------|
| 数值位宽 | SByte=i8 / Byte=u8 / Short=i16 / UShort=u16 / Int=i32 / UInt=u32 / Long=i64 / ULong=u64 / UIntPtr=u64（固定）/ Float=f32 / Double=f64 |
| **Bool** | **存 i8（0/1）**；条件用 i1；**勿用 i1 作存储**；`[Bool]` esz=1 / GEP=i8 |
| **Char** | **i32 码点**；`'…'`；`[Char]` esz=4；包装 `Character`；与 Int 同宽，`println(Char)` 专用重载 |
| **String** | 语言值仍 **ptr** → 堆头 `HaoString{i32 len, i32 cap, char data[]}`；字面量经 `hao_str_from_cstr`；`.length`/`s[i]`/`charAt`=**码点**；FFI 用 `->data`（nul 结尾） |
| 无符号语义 | LLVM 同宽底层；差异在 zext/sext、icmp u*、udiv/urem、字面量域 |
| 提升 | 同宽有符号+无符号 → 更宽有符号（如 UInt+Int→Long）；**已 64 混符号 → 编译拒绝**（须显式转换） |
| 移位 | **结果类型=左操作数一元提升**（勿用二元混合把 ULong>>Int 收成 Long）；32 位 `&31`，64 位 `&63`；`>>` 有符号 ashr / 无符号 lshr |
| 数组 esz | 8 位与 **Bool=1** / 16 位=2 / 32 位与 **Char=4** / 64 位=8 |
| 句柄/时间戳/原子 | **Long / Long?**（net/thread/sync/time）；UIntPtr 供用户指针宽算术 |
| 反射 invoke | 槽 `[Long]`；meta `Long?`；回写按 **llvmType + 有无符号** sext/zext（`Int?` 是 ptr） |
| 包装类 | 显式 valueOf；同名写 lang.X；**数值**包装有 SIZE/BYTES；无隐式装箱 |
| **Bit** | 位模式工具（≠ Boolean）；SIZE=64；静态 Int/Long 重载；**getBit→Bool**；实例包装 Long |
| **静态重载** | 同名静态方法按参数签名区分；IR 后缀 `$Int`/`$Long`…；计分初值须够大 |
| **可空拆箱** | 窄整数/Bool/**Char** 须先 wide 再 trunc；装箱 Char?→`hao_box_i32`；**try 返回 / 数组 push 须按 llvmType（ptr）存取，勿只看 kind**；**if/while/when 条件与下标禁可空**；**装箱 `==` 比载荷，`String?` 走 str_eq** |
| **boxToNullable / ??** | 已可空勿二次 box；`??` 右侧须与左侧底层兼容并 coerce；**双方可空且形态不同（Int?/Long?）禁止赋值** |
| **toString / 模板 / +=** | 可空须 `ensureNonNull`；`toStringValue` 拒 nullable（防 ptr 当 i32） |
| **Float when** | 须 `fcmp`（与 Double 一样），勿 `icmp float` |
| **when 整数** | 跨宽度（Int/Long）须 `binaryNumericPromote` + `coerce` 后再 `icmp`；勿直接用主体 llvmType |
| **有符号 / %** | 除零 panic；**MIN/-1** 须 `emitSafeSignedDivisor`（除数改 1）防 sdiv/srem 毒值 |
| **boxToI64** | 须覆盖 SByte/UShort/UInt/ULong/UIntPtr（push/pop/unwind），勿默认 ptrtoint；**TypePtr 重载**：`llvmType==ptr`→ptrtoint |
| **main():Int** | Int 已是 i32，直接 `ret i32`，勿 `trunc i64` |
| **runtime 安全** | 读缓冲上限 **INT32_MAX-1**（与 `hao_str_alloc` 同源）；parse 查 `ERANGE`；`gc_alloc`/`object_new`/数组扩容防 size 回绕；reflect field 含无符号 |
| **Pattern** | 原生 NFA 挂 **finalizer**；捕获在 **Matcher**；禁同盒 remake；search 全局步数；仍推荐 `close()` |
| **Map 扩容** | `capacity*2` 防 Int 溢出；满表且无法扩容时 **抛 Exception**（可用 `growthEnabled=false` 测） |
| **internal** | 成员 `internal` = **同 importPath**；跨包编译拒绝（negcheck） |
| **smart cast** | `if/while (x != null)` 与 `if (x == null) return` 后局部按非空；`var` 赋值使收窄失效；`!!` 对已非空为幂等 |
| **MVC / 包扫描** | `scan`/`scanWhere`/`scanWhereExact`；谓词 `Func<Type,Bool>` 可定制（注解/接口/自判）；**仅无参** `canNewInstance` 自动 new；多参 `registerNewArgs`；包须先 `import` |
| **数组 ABI** | 头 **`[pad][cap][len][esz]`=32B**（pad 保证元素区指针 16 对齐，否则 GC `GC_ALIGN=16` 漏标）；`new [T](n[,fill])`；`[...a]` 展开须按**源** esz 读再 coerce；禁 `[T]?` 展开；长度求和 `sadd.with.overflow` |
| **数组形参** | 非空数组：传槽 by-ref；**`[T]?` 按值**（与 `formatCallArg` 对齐，否则砸堆）；**extern C** 仍传数据指针；`arrayArgSlot` 勿剥到终结符 |
| **!! 赋值** | `genAssign` 支持 `x!!.field` / 链式 `!!`+`.` 作左值（`evalAssignRecv`） |
| **when 推断** | `obj.m()`/`pkg.fn()` 查真实返回类型；勿把 ≥2 后缀一律当 Unit |
| **== / throw** | 异型（如 `Bool==Int`）编译拒绝；`throw` 拒可空异常 |
| **字符串下标** | `hao_str_char_at(ptr,i64)`；Long 下标勿经 Int 截断 |
| **线程池** | `pool_new` 失败须 join 已创建 worker 再 free；`shutdown` join + 排空队列 |
| **定长长度** | `new [T](n)` 的 `n` 按有无符号 **sext/zext→i64**，勿经 Int 截断 |
| **when 表达式** | **必须有 else**（否则 alloca 未写→毒值）；语句 when 不强制 |
| **反射构造** | `@Class$new(ptr argslots)`；meta 含 `factory`+`ctorParamCount`+`ctorParamTypes`；工厂须先 `ensureInit`；槽约定同 invoke；**invoke 亦校验 arity** |
| **Func 字段** | **编译拒绝 `Func?`/`Action?`**；`obj.fn(args)` / 静态调用已支持；**字段默认可写 lambda**（new/staticinit 前 `analyzeLambdas`；同 AST 多 new 只 emit 一次 impl） |
| **hasStaticInit** | 代码生成前 `markStaticInitFlags`；否则「函数写在类前」访问静态字段会漏 `ensureInit`（null 崩溃） |
| **线程/池 GC** | `thread.start`/`pool.submit` 须先 `hao_gc_add_root(env)`；worker **勿每任务 unregister** |
| **静态 GC 根** | 类静态 `String`/对象/`[T]`/`T?` 等须 `hao_gc_add_root_slot`；`main` 调 `@hao.registerStaticRoots` |
| **remset** | **仅服务 minor**（扫 old→young）；major **禁止** enqueue；成功 major 整表丢；minor 后 `filter_live`；**勿**对所有晋升盲目 `remset_add`（屏障记真实边） |
| **while/for/块 GC 局部** | 提升 + spill 池；**块/if 尾 store null**（不 unwind）；hoist 不停嵌套裸块/内层循环 |
| **by-ref 写回** | `emitVarStore`：`byRefParam && isGcPointerType` 走 `emitHeapStore`（数组扩容写回字段） |
| **Http CL** | 头/体分隔与 CL **一律字节下标**；CL **行首锚定**；自定义头拒裸 LF / `\r\n\r\n` |
| **装箱可空比较** | 同 kind 比载荷；**`Int? == Long?` 等异宽编译拒绝**（对齐赋值） |
| **extern** | 同 C 符号重复 declare 须去重（`emittedExternLinks`）；`ptrOfStr`+`ptrOf` 曾撞车 |
| 规则 11 | 一次做对；正反向测试；不留历史包袱；**可空拒绝须覆盖算术+逻辑+复合赋值+模板+??=类型** |

### 0.5 本批收工摘要

| 项 | 内容 |
|----|------|
| **上一交付** | **v0.55.30** I0/I3 + 薄 I4 |
| **本会话** | **v0.55.49**：定位发现十五期 A13/D15/U17/V11/L15（unwind TRACE；fn/this dbg.value；catch-finally-throw；ext 毒针） |
| **下一批** | 功能轨；或完整 Hao 类型/`dbg.value` 全覆盖；或 lambda capture DI / select-in-catch |

**近期版本**：0.55.49 定位十五期 → 0.55.48 定位十四期 → 0.55.47 定位十三期 → … —— 查 [`docs/项目时间线/v0.50-0.59.md`](docs/项目时间线/v0.50-0.59.md)。

### 0.6 新会话方法论速查（修 bug / 加功能）

```
读任务(第10章) → 读相关坑(docs/坑债.md) + 防回归(0.4)
  → 对将改文件做 .bak（规则12）
  → 实现（规则8/11：不将就；正反向测试）+ public API 按 5.14 写注释
  → cmake/build_runtime + test.sh --rebuild-all [+ negcheck]
  → 绿：立刻按文档治理写（本文件摘要+README+时间线+坑债）→ 删 .bak → 下一块
  → 红：从 .bak 恢复或继续修，禁止带着崩状态写「已完成」
```

| 场景 | 要点 |
|------|------|
| 修严重 bug | 先写能复现的正/反向用例 → 再改；修完用例进 `test/suite` 或 `negcheck` |
| 加语言特性 | 文法(若需)→IRGen→runtime/stdlib→套件正反向→升版或基线→两份文档 |
| 加 stdlib 包 | 先读 **5.13**；零 SDK（**5.12**）；MVC demo 仍放最后（规则 6） |
| 发现「将就」 | 写入 [`docs/坑债.md`](docs/坑债.md) 并标注「将就」；勿假装已闭环 |

## 第 1 章 · 工作规则（用户硬性要求，每次必须遵守）

> 本节是**合同级约束**。新会话不得以「省事 / 稍后再写文档 / 先用英文 / 不 bak」为由绕过。

### 规则 1 · 全程中文

用户是中国人。对用户的**回复**、思考说明、提交说明、写入本文件与 README 的叙述，**一律中文**。代码标识符/命令可英文，说明文字用中文。**新会话也必须遵守，不可改回英文对用户输出。**

### 规则 2 · 每一步做完同步文档（完成项 + 踩坑）

用户 2026-08-06 定；**多次强调：每一步做完都要更新文档，把遇到的坑和做完的事情记下来**。

**工作流（强制）**：

```
.bak → 改代码 → 跑测试（绿）→ 立刻写文档（见下表）→ 删 .bak → 再进入下一块
```

| 文档 | 批末 / 块末必写 |
|------|----------------|
| `记忆文档.md` | 抬头版本/基线；0.4/0.5；第 5 章若有新设计；第 7/9/10 同步；**不**再粘贴完整时间线/坑表 |
| [`docs/项目时间线/`](docs/项目时间线/索引.md) | 本版专节或「本版修正」（写入对应十位合订） |
| [`docs/坑债.md`](docs/坑债.md) | **本批坑（现象→原因→解法）**；将就项进第二节 |
| `README.md` | 版本号、进度、用户可见特性或限制、下一步（坑/时间线用链接） |

- 升版：改根目录 `VERSION` → `cmake -S . -B build`（注入 `HAO_VERSION`）。
- **记录格式**：完成项写清「做了什么、验收行数」；踩坑写清「现象 → 原因 → 解法」。
- **禁止**：做完只改代码；或只更新一份；或把整段时间线/坑表再复制回本文件与 README。
- 细则见 [`docs/文档治理.md`](docs/文档治理.md)。

### 规则 3 · 改完必须跑全部测试

确认无回归（套件行数不减少、退出码全 0）再结束会话任务。改 stdlib / `.g4` / 运行时后用 `--rebuild-all`（增量不查 stdlib `.hao` mtime）。动可空/类型语义时再跑 `script/win/negcheck.ps1`。

### 规则 4 · 纯搬家式重构不改逻辑

每拆一块立即 `cmake --build` + 全量测试。

### 规则 5 · 路径与优化级别

工具链/路径用 Windows 风格；用户程序默认 **`-O2`**（Hao 帧靠 shadow；`-O2` 改善 os_block C 叶布局）。

### 规则 6 · 命名与 demo 顺序

并发关键字定名 **`haoroutine`**（不是 goroutine）；**MVC Web demo 放全部任务最后**。

### 规则 7 · 环境与产物目录

环境在 `D:\buildLang`；新依赖放 `lib/`，构建产物放 `target/`；**不要重装工具链**。

#### `target/` 布局（硬性）

| 路径 | 用途 | 寿命 |
|------|------|------|
| `output/hao.exe` | 编译器（CMake 产出，不在 target） | 开发期保留 |
| `target/win-*-haolang-<ver>/` | **仅** `package.ps1` 正式发行包 | 发版保留；旧版可删 |
| `target/test/` | **所有**测试临时产物（suite 缓存、negcheck、modsmoke、stdout） | 脚本用完清空临时子项；允许保留 `target/test/cache/suite.exe` 做增量 |
| `target/` 根下其它文件 | **禁止**（`.txt` / `.ll` / 探针 exe / 旧发行包垃圾） | 发现即删 |

**约定**：
1. 测试脚本（`test.sh` / `negcheck.ps1` / 手工冒烟）产物必须写到 `target/test/...`，禁止往 `target/` 根乱扔。
2. 跑完应清空临时目录（negcheck 整目录删；test.sh 清 `out/` 与一次性 exe）；需要时再生成。
3. 本地仓默认在用户目录 `~/.hao/repo`（`HAO_REPO`），**不**放 `target/`。测试可用 `HAO_REPO=target/test/haorepo` 并在收工删除。

### 规则 8 · 不能将就 / 架构以 Java/C# 为参照

机制与目标形态冲突且影响后续扩展时，实现根本机制，不打补丁。例：`Object` 根替代 Hashable 凑合；`Iterable<T>` 替代 `toArray()` duck-typing；CRT 层用 `lib/sysroot/win-*`，不「随便拷一份 SDK」。判断：是否有大量后续代码依赖它。

### 规则 9 · 高内聚低耦合

多处共用的抽公共模块（如 `hao_dl_*` / `hao_win_*`），禁止复制粘贴。

### 规则 10 · 运行时 / stdlib 零外部开发依赖

详见 **5.12**。禁止 `#pragma comment`、禁止依赖 SDK 系统 API 导入库、禁止随意 `windows.h` 引出 `__imp_*`。系统 API 用 **`hao_dl_*`**；CRT 由 `lib/sysroot/win-<arch>/lib` 提供。发行包只用相对路径定位 stdlib/CRT。

### 规则 11 · 一次做对 / 正反向测试 / 不留别名包袱

- **禁止**「别名凑合、半套类型、只做正向冒烟、静默吞错当成功」。
- **同类缺口一次补齐**（动数值位宽就连 esz/句柄/反射一起改）。
- **测试必须含**：常见路径 + **正向** + **反向/边界**（截断、溢出、重载歧义、不可赋值、可空误用）；先暴露再修。反向集中脚本：`script/win/negcheck.ps1`。
- 批末把踩坑写入 [`docs/坑债.md`](docs/坑债.md)；若本批用了权宜之计，**必须**标「将就」并列入 [`docs/坑债.md`](docs/坑债.md) 将就债，不得假装闭环。

### 规则 12 · 改源码先备份 `.bak`，收工清除

> 教训：v0.32 曾**无备份整文件覆盖** `IRGenLiteral.cpp`，冲掉 when/字面量/模板，无 git 只能手写恢复（见 [`docs/坑债.md`](docs/坑债.md) **AC**）。

**强制流程**：

1. **改之前**：对每个将改的原文件复制同目录 `原名.bak`（例：`IRGenLiteral.cpp.bak`）。大文件 / 多文件改动尤须如此。
2. **改坏了**：用 `.bak` 覆盖回原文件再重来；禁止在半毁文件上继续「猜着补」。
3. **全部验收通过 + 两份文档已写完**：删除本批产生的**全部** `.bak`（勿把备份提交进发行思路；勿残留到下会话）。
4. **禁止**：无 bak 就对大源文件做整文件 Write/覆盖；会话结束仍留着 `.bak`。

PowerShell 示例：

```powershell
Copy-Item src\irgen\IRGenLiteral.cpp src\irgen\IRGenLiteral.cpp.bak -Force
# …修改与测试…
Remove-Item src\irgen\*.bak -ErrorAction SilentlyContinue   # 收工清理（按实际路径）
```

---

## 第 2 章 · 版本时间线

> **正文已迁出**，避免本文件无限膨胀。

| 查什么 | 去哪 |
|--------|------|
| 一句话索引 + 合订链接 | [`docs/项目时间线/索引.md`](docs/项目时间线/索引.md) |
| v0.50～当前专节 | [`docs/项目时间线/v0.50-0.59.md`](docs/项目时间线/v0.50-0.59.md) |
| v0.40～0.49 | [`docs/项目时间线/v0.40-0.49.md`](docs/项目时间线/v0.40-0.49.md) |
| v0.30～0.39 | [`docs/项目时间线/v0.30-0.39.md`](docs/项目时间线/v0.30-0.39.md) |
| v0.20～0.29（含 0.21.x） | [`docs/项目时间线/v0.20-0.29.md`](docs/项目时间线/v0.20-0.29.md) |
| v0.10～0.19 | [`docs/项目时间线/v0.10-0.19.md`](docs/项目时间线/v0.10-0.19.md) |
| v0.5～0.9 | [`docs/项目时间线/v0.05-0.09.md`](docs/项目时间线/v0.05-0.09.md) |

**当前**：先查 [`docs/项目时间线/索引.md`](docs/项目时间线/索引.md)；近版专节在 [`v0.50-0.59.md`](docs/项目时间线/v0.50-0.59.md)。勿把旧合订「本版」当 HEAD。

维护：升版时在对应合订**文首**插专节，并更新索引表一行（见 [`docs/文档治理.md`](docs/文档治理.md)）。

## 第 3 章 · 已实现功能（特性速查）

> **详文**：[`docs/hao语法.md`](docs/hao语法.md)。**用户示例**：[`haolang-example/`](haolang-example/) + README 摘要 + 语法文档。本表只标状态，避免与 README 双份维护。

| 类别 | 状态 | 要点 |
|------|------|------|
| 基础类型 / 运算 / 控制流 / 函数 | ✅ | 含 Byte/Char、位运算、when、重载 |
| 类 / 接口 / 继承 / Object 根 | ✅ | 虚表、extends/implements、super |
| static / enum / init() | ✅ | 静态构造器、包 init |
| 泛型 / 泛型接口 | ✅ | 单态化；`Iterable`/`Iterator` |
| 空安全 / 异常 / Lambda | ✅ | `T?`、try/catch、闭包 |
| 反射 / 注解 / Delegate·Action·Func | ✅ | invoke；`@interface` |
| GC / haoroutine / channel / select | ✅ | [IR与GC契约](docs/IR与GC契约.md)；channel 载荷 Long |
| 多文件包 / extern / 外部库 | ✅ | 目录即包；`-l`/`@link` |
| 工具链子命令 | ✅ | run/build/mod/test/fmt/… |
| 泛型约束 `where` / 自动属性 / 接口默认方法 | ⬜/🔸 | 见第 9～10 章 |


## 第 4 章 · 标准库 / 系统库清单（速查）

> **用法**：`import <pkg>`；`fmt` / `object.*` / `lang.*` 隐式预导入。  
> **详文**：各包 `stdlib/src/<pkg>/` 文件头注释；语法侧见 [`docs/hao语法.md`](docs/hao语法.md)。  
> **新增包**：见第 7 章「新增标准库」+ **5.13** / **5.14**。

| 包 / 路径 | 一句话 |
|-----------|--------|
| `lang` / `lang.Bit` | 装箱包装；位模式工具（≠ Boolean） |
| `hash` | `hashOf` / `equalsKey`（Map/Set 用） |
| `math` / `random` / `assert` / `text` | 纯 .hao 工具 |
| `collections` | List/Map/Set/Queue/Stack + 树/LHM/CHM… |
| `exception` / `fmt` | Exception 基类；println 重载 |
| `os` | 文件/Path/环境/FileStream |
| `sync` / `thread` / `channel` | Mutex/原子；线程池；channel+select |
| `net` | TCP/UDP + Http/MVC + Html + staticFiles（dynload） |
| `reflect` / `time` | 类型自省/invoke；时间格式化 |
| `regex` / `json` / `gc` | Pattern；JSON；GC.collect/summary |
| `testing` | `hao test` 的 `T` |

**禁止再开**独立 `bits` / `path` / `io` 包（能力已并入 `lang.Bit` / `os`）。


## 第 5 章 · 关键实现规则（设计决策）

> 接手前必看。理解这些"为什么"，才能在不破坏设计的前提下改代码。

### 5.1 类型系统：值槽与数组元素宽度

- **Int=`i32`**、**Long=`i64`**、**Short=`i16`**、**Byte=`u8`（无符号 0～255）**、**Float=`float`**、**Double=`double`**、**Bool=`i8`**、String/Class/Interface/Array/Func=`ptr`。
- **对象字段仍统一占 8 字节槽**（窄值存于槽内）；换实现简单。
- **数组**：头 **`[pad][cap][len][esz]`=32B**（pad 保证元素区 16 对齐，服务 `GC_ALIGN`），返回指针指向元素区；esz∈{1,2,4,8}（**Bool=1**）。`hao_array_new(len, esz)`。
- 空安全：引用类型 null=指针 0；值类型可空装箱为指针（Short/Byte/Bool 经宽化走 `hao_box_i32`；Long 走 `hao_box_i64`）。
- **位运算**：有 Long→Long，否则至少 Int；`lang.Bit` 位模式固定 Long，**getBit 返回 Bool**。

### 5.2 虚表：全局槽位编号

接口方法与继承体系中的虚方法在**全局编号空间**分配槽位，整条继承链上的同名方法共用一个槽位。
- 接口方法编号 = 接口内声明顺序；类虚方法编号 = "最初声明该方法的类"决定（沿继承链找根）。
- **坑**：曾用 `ownerClass.method` 做 key，覆写时 owner 被改成子类名，导致 `Dog.speak`/`Cat.speak`/`Puppy.speak` 各占槽位，父类型调用取到 null。
- **所有类都生成虚表**（至少 1 槽），虚表地址兼作运行时类型标识（is/as/catch 用 typeid 列表；也为精确 GC 铺路）。

### 5.3 泛型：单态化（monomorphization）

与 C++/Rust 同路线，不做类型擦除，每个类型组合生成一份专用代码，零开销。
- 实例名用 `$` 拼接（`$` 不是 HaoLang 标识符合法字符）：`Box<Int>`=`Box$Int`、`Pair<String,Int>`=`Pair$String$Int`、`Box<Box<Int>>`=`Box$Box$Int`。`Type::monoName` 对基本类型返回固定名。
- 泛型函数类型参数**按实参推断**（调用处不写 `<T>`）；实例化可能在任意位置触发（含另一个实例化内部），用**待生成队列迭代到稳定**。
- 泛型函数名当值不能直接用（返回类型 Unknown），需先以具体类型调用实例化。
- 泛型方法（v0.9.0）：方法级类型参数在调用点从 lambda 返回类型推断，生成 `List$Int.map$String` 专用方法并静态调用。

### 5.4 GC ↔ IR（摘要；详文唯一权威）

> **详文**：[`docs/IR与GC契约.md`](docs/IR与GC契约.md)（埋点表、流程图、R1–R9 规则、指标、检查清单）。  
> 勿在 README / 时间线再复制整份契约；坑见 [`docs/坑债.md`](docs/坑债.md) BI～CE。

**当前实现（v0.55.10）一句话**：**可达性主路径已交付**（根→三色标→sweep 白；过时「全保守栈 / 无并发 mark」口径已废）+ **混合屏障** + **精确根按可达性推进**（提升 + spill 池 + 块尾清槽 + continue 纪律/condL 补清 + 表达式面/`?.`/`??`/装箱 `rootGcOperand`；return unwind）+ **诚实双轨根** + remset **仅 minor** + C 皮带（禁挂根前 `is_heap_ptr`；channel 用 `add_root_if_heap`）+ 软 STW + **mark worker**。

| 层 | 要点 |
|----|------|
| IR safepoint | 循环 + 函数/lambda/staticinit/工厂 + 合成循环 |
| IR 堆写 | 槽地址 `hao_gc_barrier` → store（混合） |
| IR 精确根 | 局部/参数 + spill 池 + **块尾清槽** + 表达式面调用/串接/字面量根 + when/catch/select/new/aspread |
| Runtime STW | 始终 shadow；os_block → +GPR+C 叶；仅 Hao safepoint → shadow-only |
| 一轮 GC | setup→软根 STW→assist+worker drain→终止→sweep/abort |
| 观测 | `heapBytes`；`markWorkerSteps`；`markAbortCycles` |

**排期 / 架构选择（勿写成「做不了」）**：concurrent sweep / span（**排期**）；Win SuspendThread（**禁止**）；LLVM stackmap（**架构不用**，shadow 替代）。

### 5.5 Lambda / 闭包

- 闭包 = 堆对象 env `[fnptr|cap0|cap1...]`（`hao_object_new` 分配，GC 回收）。槽 0 = 实现函数指针，签名统一 `ret(env, args...)`——无捕获/有捕获/lambda/顶层函数包装器调用约定一致。
- lambda impl 是**顶层函数**（`@lambda$N(ptr %env.arg, T %p.arg...)`），不能嵌宿主函数体。`IREmitter::push/popFunctionState` 切换缓冲生成整个 define 再统一追加（**pop 后必须 `seekp(0,ios::end)`**，否则写指针回到开头覆盖主函数体）。
- 可变捕获（外层 var/参数）装箱为 8 字节堆 cell 共享；不可变 val/this 按值捕获。
- 自由变量分析：`declared` 集合只放 lambda **自己的形参**——宿主形参正是要捕获的自由变量。
- `fieldPtr()` 内部会 nextTemp，调用顺序必须先 fieldPtr 再取目标寄存器（否则 SSA 编号倒退）。
- 顶层函数当值：生成只含 fnptr 的 env（指向 `@foo$wf` 包装器），普通 `foo(x)` 仍走直接 call。

### 5.6 异常

- 运行时用 setjmp/longjmp 异常帧链。**setjmp 必须在用户函数自身栈帧里 IR 直接 `call`**，不能放进会返回的 C 辅助函数（longjmp 跳回已失效栈帧会段错误）。
- Windows x64 用双参 `_setjmp(buf, @llvm.frameaddress(0))`，Linux 用单参 `setjmp(buf)`，都标 `returns_twice`。
- return/break/continue/throw 穿过 finally 靠 `unwindReason/Ret/StopAddr` 槽 + cleanup 链；帧带唯一 id，`hao_try_end(id)` 只在栈顶匹配时弹，幂等。
- catch 按虚表 + `hao_type_is` + typeids 列表匹配（typeids 含该类型及所有子类，天然支持子类捕获）。

### 5.7 包 / 多文件（v0.6.0）

- Go 风格"目录即包、整盘编译"：目录内所有 `.hao` 的 `package` 名必须一致；整个 import 闭包一次生成一份 IR。
- 入口语义：传文件=只编译列出的文件；传目录=编译该包全部 `.hao`。
- import 查找：先相对入口目录找子目录，再找编译器自带 `stdlib/src/<path>`。
- 非 main 包顶层符号 IR 名加前缀（`/`→`$`，如 `util/strings` 的 `join`→`@util$strings$join`）；main 包前缀为空。
- 两阶段收集（阶段 A 登记类型名、阶段 B 解析成员）解决跨包类型前向引用。

### 5.8 extern C 函数声明（v0.8.0）

- 语法：`extern func f(x:Int):Unit = "c_name";`——`extern` 是修饰符（可 `public extern`）；`= "c_name"` 指定 IR 符号名（省略用短名），无函数体、以 `;` 结尾。
- extern 的 `irName` 直接设 `"@"+linkName`（**不带包前缀**），跨包调用直接 `call @hao_println_int`，零开销。
- `collectFunctionSignatures` 收集；`generate()` 遍历全局符号表对每个 extern 拼 `declare`。
- **与内建运行时声明去重**：`IREmitter::isRuntimeDeclared(linkName)` 解析内建 `declare ... @name(` 的符号名，命中则跳过该 extern 的 declare（符号仍注册可调用）。
- 未声明的 C 符号不在编译期报错，在**链接期**由 lld 报 undefined symbol（期望行为）。
- extern 不能是泛型（实践上 extern 不该有类型参数）。

### 5.9 函数重载（v0.9.0）

- `overloads_` 表（`map<内部名, vector<SymbolPtr>>`）；`collectFunctionSignatures` 签名相同才报"重复定义"。
- **IR 符号名带签名后缀**（`@area$Int$Int`），`overloadSuffix` 递归展开数组/函数/类型参数；extern 重载靠显式 `= "linkName"` 区分，不加后缀。
- `selectOverload`：过滤参数个数 → 每实参精确 0 分 / 可赋值（Int→Double）1 分 → 取最低分唯一候选。
- `genFunction(fn)` 必须按 AST 签名从 `overloads_` 选出对应符号（否则拿第一个重载的参数生成函数体，body 引用错参数名报"未定义"）。

### 5.10 集合哈希（v0.9.0）

Map/Set 泛型单态化后 `hashOf(key)` 顶层**重载**解析自动选对版本——绕开"泛型方法里无法按 K 分派"的难题。`Hash.hao` 声明 extern hash 函数 + 顶层重载 `hashOf`/`equalsKey`（Int/Double/Bool/String）。

### 5.11 系统库模式（v0.12/v0.13）

- `.hao` 包声明 `public extern func` = C 符号；C 实现放 `stdlib/runtime_<name>.c`（跨平台，尽量用 C 标准库/`__atomic` 内建，避免平台分支）。
- 跨 C 边界返回字符串必须 `gc_alloc` + 拷贝（getenv 静态缓冲坑）。
- 锁/计数单元复用 `Long?` 装箱（8 字节 GC 块），C 侧当 `int64_t*` 原子内存。
- `build_runtime.ps1` 自动发现 `runtime_*.c`，新增模块无需改脚本。
- **系统 API 依赖**（网络/线程/时间等）须遵守 **5.12**，不得靠导入库链接。

### 5.12 运行时系统库 · 动态加载（零外部开发依赖）（用户 2026-08-07 定 · **v0.21.x**）

> **问题（已解决）**：旧 `runtime_net.c` 用 `#pragma comment(lib,"ws2_32")` / SDK `ws2_32.lib`。无 SDK 机器链接失败；交叉/多架构可能 `machine type arm conflicts with x64`。v0.21.0 修了 net；v0.21.1 补齐其它模块 + CRT 层。
>
> **两层模型**：
> - **系统 API 层**：只依赖目标机**预装 DLL**（运行时 dynload），编译期不产生 `__imp_*`。
> - **CRT 层**（仅 Windows MSVC ABI）：`libcmt` 及其 DEFAULTLIB 是链接器硬需求 → 按 arch 放入 `lib/sysroot/win-<arch>/lib`，与 linux musl sysroot 对称。

#### 硬性验收标准

| # | 要求 | 说明 |
|---|------|------|
| 1 | **系统 API 零导入** | runtime 无 `#pragma comment(lib,"ws2_32")`、无 `/DEFAULTLIB:ws2_32`、无 `U __imp_*`。directives 仅 CRT（`libcmt`）。 |
| 2 | **只依赖系统预装动态库** | Windows → `hao_dl_open`；Linux musl 静态 → 直调 libc。 |
| 3 | **对外 API 不变** | `hao_net_*` / `hao_thread_*` / … 与 `.hao` 包零改动。 |
| 4 | **规避架构冲突** | 不链 SDK 分架构 API 导入库；CRT `.lib` **按目标 arch 分目录**，禁止混放。 |
| 5 | **多平台同一模式** | 公共 `hao_dl_*` +（Windows）`hao_win_*`；POSIX 直调。 |
| 6 | **无 VS 可链（Windows）** | 发行包含对应 arch 的 CRT 最小集；`DriverLink` 自动 `-L`。 |

#### 实现要点

1. **PEB 引导**（`runtime_dynload.c`）：无导入解析已加载的 kernel32，再提供 `hao_dl_*`。
2. **`runtime_winapi.c`**：kernel32 API 函数指针表；gc/thread/time/print 只调封装。
3. **禁止** `#include <windows.h>` 进公共头（会拉 `uuid.lib` DEFAULTLIB + `__imp_*`）。
4. **CRT 采集**：`script/win/fetch_winlibs.ps1 -Target win-amd64|win-arm64`（脚本须 **UTF-8 BOM**，否则 PS5.1 吃掉 `$变量`）。
5. **与用户 `@link` 边界**：stdlib dynload；用户显式 `@link("ws2_32")` 仍需本机导入库。

#### 多架构怎么做（推导）

| 目标 | 策略 |
|------|------|
| win-amd64 | 当前宿主默认；CRT 最小集进包 |
| win-arm64 | 同结构换 arm64 `.lib`；runtime 已用 `x18` 读 TEB；打包/交叉时再 `fetch_winlibs -Target win-arm64` |
| linux-amd64/arm64 | 已有 musl `fetch_sysroot.ps1`（头文件+静态 libc） |
| darwin-* | 不能重分发 Apple SDK；链接依赖本机 Xcode，文档标明 |

### 5.13 标准库分包规范（v0.23.0 定 · 用户硬性）

> **原则**：按**职责域**建包，禁止「一个小功能一个包」导致 import 碎片化。

| 域 | 包 | 放什么 | 不放什么 |
|----|-----|--------|----------|
| 语言核心类型 | **`lang`** | Integer/Boolean/String/Double/**Bit**；常量；后续字节缓冲/编码字符集工具 | 文件、网络、集合算法 |
| 操作系统 / 文件 | **`os`** | 文件读写、环境、进程、**Path**、文本 I/O 别名；后续二进制读可挂此或 `lang` 缓冲 | 纯字符串算法（→ text） |
| 文本算法 | **`text`** | split/join/pad…（不碰文件系统） | 路径、读写 |
| 正则 | **`regex`** | `Pattern`/`Matcher`（NFA；体量够大独立包） | JSON 解析本身 |
| JSON | **`json`** | Fastjson2 风 `JSON`/`JSONObject`/`JSONArray`/`JSONPath` | 文件流（→ os） |
| 数学 | **`math`** | `class Math` + 顶层函数 | 位模式（→ lang.Bit） |
| 集合 / 哈希 | **`collections`** / **`hash`** | 容器；散列协议 | — |
| 并发 / 网络 / 时间 / 反射 | **`sync`/`net`/`time`/`thread`/`reflect`** | 各域专职；Http 同 `net` 包 | — |
| GC 详情 / 手动回收 | **`gc`**（v0.34） | `GC.collect`、存活字节/阈值/块数/收集次数、`summary` | 精确堆/分代实现（属 runtime，非本包） |

**硬规则**：
1. **禁止**再新增 `bits`/`path`/`io` 这类薄包装独立包；同类能力并入上表职责域。
2. **允许** `package X` + `class X`（如 `math.Math`）；编译器须支持 `import x; x.X.member`（见 [`docs/坑债.md`](docs/坑债.md) **M**）。
3. 包装类优先放 **`lang`**（隐式预导入），用户少写 import。
4. 同包多文件按主题拆分（如 `os/os.hao` + `os/Path.hao`），**不要**为拆分而新包。
5. 新能力先问「落在哪个域」；只有跨域、体量够大才开新包（如未来完整 `json`/`regex`）。
6. **GC  introspection**（v0.34）：包 **`gc`**（`GC.collect` / 统计 / `summary`）；勿塞进 `lang`（非核心类型）或 `os`。

### 5.14 标准库注释规范（v0.34.0 定 · 用户硬性）

> **原则**：打开 `stdlib/src/**/*.hao` 即文档。人不能靠猜测和经验调用 API。  
> **适用范围**：本批起——**所有新建** `.hao` 的 public API，以及**本批改动触及**的 public 方法，必须按本规范写注释。未触及的存量文件列入中期「注释清债」，禁止假装全库已注释完。

#### 强制层级

| 层级 | 必须写清 |
|------|----------|
| **文件头** | 包/文件职责；对标（Java/Go 等，若有）；线程安全；失败语义（抛错 / 返回 null / panic）；**不是什么**（防误解，如 CHM ≠ Java 8 CAS 桶锁） |
| **类型** | 角色一句话；关键不变式（tombstone、插入序链、红黑性质等） |
| **每个 public 方法** | 见下方模板 |

#### public 方法注释模板（中文）

```
// <做什么——勿复述标识符>
// @param name  含义；null/? 约定
// @return      含义；缺失/空时的返回值
// 复杂度：O(...)（可写摊销）
// 异常/失败：何时 throw / 返回 null
// 与对标差异：…（若有）
```

#### 禁止

- 无注释的 public API
- 「见源码」「同上」搪塞
- 仅英文说明面向用户的语义（标识符/符号名可英文）
- 注释与行为不符（改代码必须改注释）

#### 与方法论关系

第 0.6 工作流在「实现」步增加：**写/补 public 注释 → 再测**。批末自检含「本批新/改 API 注释齐全」。

### 5.15 项目清单与包管理（v0.38.0 定 · 用户硬性）

> **不用 `hao.mod`**。**工程清单**文件名 **`haoproject.json`**；**发布包元数据**文件名 **`haopkg.json`**（仓内 `<module>/<version>/`）。二者角色不同，**不统一成同名**。  
> **命令仍叫 `hao mod`**（对标 go mod 的 UX），不是文件扩展名。  
> 用户向说明见 [`docs/hao命令.md`](docs/hao命令.md) **§4.0**。

#### 核心思想（终局不变）

1. **全局统一本地仓（`HAO_REPO`）**：默认 `~/.hao/repo`；布局 `<module>/<version>/`；**不进项目目录**。
2. **`localReferences` ≠ 包**：本地项目互相引用（对标 C# ProjectReference），不下载、不进本地仓。
3. **源仓以全局为主**：`HAO_REGISTRY` / 项目 `registry.additional`；`includeDefault` 控制官方 URL；**禁止项目删光系统默认源**的语义被误用。
4. **优先级（源）**：`HAO_REGISTRY` > 全局 config（预留）> 项目 `additional` > 内置 `https://pkg.haolang.org`。编译读取只看 `HAO_REPO`。
5. **stdlib 永远随工具链**（`stdlib/src`），**不走 registry / 本地仓**。
6. **依赖键用完整模块路径**（如 `github.com/foo/bar`）；源码 `import` 短名仍由包目录/`package` 决定。
7. **测试规范**：`HAO_REGISTRY=http://私服`（数据根 `repo/RegisterRepo`）+ `HAO_REPO=repo/LocalRepo`；测完关私服。无 `HAO_PACKAGES_CACHE`。

#### 第 0 层（✅ v0.38.0 已交付）

| 能力 | 说明 |
|------|------|
| 文件 | `haoproject.json`：`project.name/module/version`；可选 `main`/`target`/`output`/`haoVersion` |
| `localReferences` | 字符串数组；相对清单目录；作为 **额外 import 搜索根** |
| 命令 | `hao mod init [模块路径]` |
| build/run | 入口旁有清单则加载；未知字段（`dependencies` 等）**忽略保留** |
| 测试 | `test/modsmoke`（app → `../greeter`） |

最小示例：

```json
{
  "project": {
    "name": "myapp",
    "module": "github.com/you/myapp",
    "version": "0.1.0",
    "main": "main.hao"
  },
  "localReferences": ["../common"]
}
```

#### 第 1 层（✅ v0.39.0 已交付）

| 能力 | 说明 |
|------|------|
| `dependencies` | 精确版或 `^`/`~`/`>=`/`=`（**v0.45**）；lock 只存解析后的精确版 |
| `hao mod tidy [dir]` | 本地仓先查 → 未命中从源拉取写入 `HAO_REPO` → 写 lock（sha256） |
| 源仓库 | `HAO_REGISTRY` > 项目 `additional` > 官方 URL（file / `file:` / HTTP zip） |
| 本地仓布局 | `<HAO_REPO>/<module>/<version>/`（与远程一致） |
| `replace` | → 本地路径（不进本地仓）或 `module@约束` 再查源选版 |
| build | 有依赖时自动 ensure（缺 lock/本地仓则 tidy）；校验 sha256 |
| 测试 | `depsapp`（源→仓 / 纯本地命中）+ semver/graph + HTTP（RegisterRepo） |

源/私服夹具布局：`<根>/<module>/<version>/`（内含包源码目录，如 `utilpkg/*.hao`）。

#### 后续分层

| 层 | 内容 | 状态 |
|----|------|------|
| **第 2 层 A1** | semver 范围选版 | ✅ v0.45 |
| **第 2 层 A2** | 子依赖、冲突、`exclude`、`hao mod why` | ✅ v0.46 |
| **第 2 层 A3** | HTTP(S) zip 拉取、`HAO_TOKEN`/`HAO_PROXY` | ✅ v0.47 |
| **配套** | 全局 `config.json`；完整 JSON Schema | 未做 |

#### 环境变量

| 变量 | 状态 |
|------|------|
| `HAO_REGISTRY` | ✅ 源仓（`http(s):` / file） |
| `HAO_REPO` | ✅ 本地仓（默认 `~/.hao/repo`；测试规范 `repo/LocalRepo`） |
| `HAO_TOKEN` | ✅ Bearer（HTTP 拉取） |
| `HAO_PROXY` | ✅ 命名代理 URL（WinHTTP） |
| `HAO_CONFIG_PATH` | 预留未读 |

#### 硬规则

1. 禁止再引入 `hao.mod` / 自研 mod 文法。
2. 禁止把远程包装进项目 `vendor/`（除非将来显式 `hao mod vendor`，另议）。
3. HTTP 仓约定：`GET {registry}/{module}/versions.json` + `GET {registry}/{module}/{version}.zip`；本地 file 优先。
4. 改清单字段语义须升版并更新本小节 + README + [`docs/hao命令.md`](docs/hao命令.md)。
5. 测试产物遵守规则 7（`target/test/`）。

#### 用户长文（勿把教程灌回本章）

- 命令 / 包管理 / `hao test`：[`docs/hao命令.md`](docs/hao命令.md)
- 语法语义与对标：[`docs/hao语法.md`](docs/hao语法.md)

### 5.16 单元测试协议（v0.42 · 对标 go test · 废将就）

> **禁止**再把 `hao test` 定义成「安静跑业务 `main`」。要跑程序用 `hao run` / `hao build`。  
> 语言集成基线仍是 `test/suite` + `script/test.sh`（与单元测试框架分工）。

| 规则 | 内容 |
|------|------|
| 文件 | 同包 `*_test.hao`；**普通** `hao build`/`run` **永不**编译这些文件 |
| 函数 | 顶层 `func TestXxx(t: testing.T)`：`Test` 后首字母大写；单参 `testing.T`（或 `import testing.*` 后的 `T`） |
| 包 | 第 1 层仅同包测试；`xxx_test` 外部测试包 → 第 2 层 |
| 入口 | test 二进制由工具生成 `__hao_test_main.hao` 提供 `main`；**业务 `main` 在 testMode 下跳过登记/codegen** |
| 判定 | `t.failed()` 或未捕获异常 → 用例 FAIL；进程码非 0 当有失败 |
| 产物 | `target/test/hao-test/`（规则 7） |
| CLI | `hao test [路径...] [-v] [-run regexp]`；无 Test 则报错退出（不回退跑 main） |
| 本批不做 | 并行 / 子测试 / bench / cover / `@Test` 注解扫描 |

实现触点：`BuildOptions.testMode`、`PackageResolver` 过滤、`IRGen::testMode_`、`stdlib/src/testing`、`src/tool/HaoTest.cpp`。用户长文：[`docs/hao命令.md`](docs/hao命令.md) §5。

---

## 第 6 章 · 踩坑与将就债（指针）

> **唯一正文**：[`docs/坑债.md`](docs/坑债.md)

| 内容 | 位置 |
|------|------|
| 踩坑表（现象→原因→解法） | 坑债 · 第一节 |
| 将就债清单 | 坑债 · 第二节 |
| 构建/用户可见速查 | 坑债 · 第三节 |

**批末必补**坑债；将就项必须标出并进第二节，不得假装闭环。

## 第 7 章 · 代码地图（看哪里 / 改哪里 / 新增哪里）

```
D:\buildLang
├── docs/                       文档治理 / 坑债 / 时间线；**hao语法.md / hao命令.md**（打进发行包）
├── haolang-example/            **用户向示例库**（01-hello…07-testing；打进发行包 examples/）
├── 记忆文档.md                 工作规则与设计决策（权威）
├── README.md                   用户向用法与示例入口
├── src/                        编译器（C++17）
│   ├── main.cpp                hao 命令入口/子命令分发
│   ├── HaoVersion.h            版本/Platform
│   ├── ast/                    HaoLangLexer.g4 / HaoLangParser.g4 / generated/
│   ├── util/                   FileUtil/PathUtil/ConsoleUtil/StringUtil/**Sha256**
│   ├── mod/                    **项目清单** HaoProject + ModResolve + SemVer + HttpFetch（v0.38～0.47）
│   ├── tool/                   **hao test**（v0.42）/ **hao fmt**（v0.44 缩进）
│   ├── driver/                 编译驱动
│   │   ├── Driver.cpp          build/run 编排（applyHaoProjectToOptions）
│   │   ├── DriverPaths.cpp     findClang / findRuntimeLib / findSysroot / **findWinCrtLibDir**
│   │   ├── DriverCompile.cpp   .hao → .ll
│   │   ├── DriverResolve.cpp   **包/import 解析**（entry→localRefs→deps cache→stdlib）
│   │   └── DriverLink.cpp      .ll → .exe（`-L` 含 Win CRT 最小集 / 交叉编译）
│   ├── sema/                   Type.h / SymbolTable.h / Diagnostic.h
│   └── irgen/                  同一 class IRGen 的方法分散在多个 .cpp
│       ├── IREmitter.h         文本 IR 输出、寄存器/标签、字符串池、运行时声明
│       ├── IROps.h/.cpp        通用指令收口（load/store/call/br/…；I3 !dbg 挂此）
│       ├── IRGen.h             facade；ops_ 转发；GC 封装声明
│       ├── IRGenValue.cpp      值/coerce/GC spill/根
│       ├── IRGenExpr.cpp       表达式
│       ├── IRGenControl.cpp    控制流
│       ├── IRGenLiteral.cpp    字面量
│       ├── IRGenExcept.cpp     异常
│       ├── IRGenClass.cpp      类/方法
│       ├── IRGenLambda.cpp     闭包
│       └── IRGen.cpp           函数/模块顶层
│       ├── IRGen.h             IRGen 类声明（所有 gen* 方法、状态字段）
│       ├── IRGenClass.cpp      类/接口/继承/虚表/泛型类单态化（~980行）
│       ├── IRGenExpr.cpp       表达式/赋值/方法调用/空安全/new；**含 genPostfix（pkg.Class 静态）**
│       ├── IRGenValue.cpp      loadVar/coerce/fieldPtr/arrayElemPtr/类型推断
│       ├── IRGenControl.cpp    语句/变量声明/if/while/for-in/when
│       ├── IRGenExcept.cpp     return/unwind/try-catch-finally/throw
│       ├── IRGenLambda.cpp     闭包：自由变量/env/impl/顶层包装器
│       └── IRGenLiteral.cpp    数组字面量/when 表达式/字面量/模板串
├── stdlib/                     运行时（C）+ HaoLang 标准库包
│   ├── runtime_internal.h      共享头（gc_alloc、panic 声明）
│   ├── runtime_gc.c            GC v3 精确堆/分代/屏障/根槽 + STW + naked 垫片
│   ├── runtime_string.c        字符串拼接/转换/长度/比较
│   ├── runtime_array.c         定长+动态数组 new/len/cap/push/pop
│   ├── runtime_object.c        对象分配、is/as 类型判定
│   ├── runtime_box.c           值类型可空装箱/拆箱
│   ├── runtime_exception.c      setjmp/longjmp 异常帧
│   ├── runtime_print.c         fmt 输出 + Windows 控制台 UTF-8 初始化
│   ├── runtime_panic.c         null/除零/越界/转型 panic
│   ├── runtime_hash.c          Map/Set 哈希（v0.9.0）
│   ├── runtime_object.c        hao_object_new/is + Object 根默认 hashCode/equals/toString（v0.16.0）
│   ├── runtime_os.c            os 系统库（v0.12.0）
│   ├── runtime_sync.c          sync 并发原语（v0.13.0）
│   ├── runtime_dynload.c       动态库加载 hao_dl_*（PEB 引导，v0.21.x）
│   ├── runtime_winapi.c        Windows API 惰性封装 hao_win_*（v0.21.1）
│   ├── runtime_net.c           net 网络库（v0.14.0；v0.21.0 Windows 动态加载 ws2_32.dll）
│   ├── runtime_reflect.c       反射元数据 + 字段读写（v0.19.0）
│   ├── runtime_time.c          时间/时区/格式化（v0.19.0）
│   ├── runtime_thread.c        线程/线程池（v0.19.0，Win pthread）
│   ├── runtime_regex.c         正则回溯引擎（v0.28）
│   ├── runtime_fs.c            FileStream（v0.28）
│   └── src/
│       ├── regex/              Pattern/Matcher（v0.28）
│       ├── json/               JSON/JSONObject/JSONArray/JSONPath（v0.28）
│       ├── os/                 + FileStream.hao
│       └── net/                Http + Mvc + **Html.hao（v0.49）**
│   └── src/                    **HaoLang 标准库包（发行版必须含，见 package.ps1）**
│       ├── collections/        List/Map/Set/Queue/Stack/Hash/Iterable.hao
│       ├── object/             Object.hao（根父类，v0.16.0）
│       ├── fmt/                fmt.hao
│       ├── exception/          Exception.hao
│       ├── os/                 os.hao + Path.hao（含文本 I/O）
│       ├── lang/               Integer/Boolean/String/Double/**Bit**/**Character**/native
│       ├── math/               Math.hao（含 class Math）
│       ├── hash/ text/ random/ assert/
│       ├── sync/               sync.hao
│       ├── net/                net.hao + Http.hao + Mvc.hao
│       ├── reflect/            reflect.hao（v0.19.0；v0.29 invokeFloat/ptrOf）
│       ├── time/               time.hao（v0.19.0）
│       └── thread/             thread.hao（v0.19.0）
├── script/                     
│   ├── setup_env.ps1           新机器入口（→ win/setup_env.ps1）
│   ├── test.sh                 测试套件
│   ├── haoreg_server.py        私服（默认 --root repo/RegisterRepo）
│   └── win/                    Windows PowerShell（env/setup/fetch_*/build_runtime/package/negcheck）
├── env.ps1                     会话入口（→ script/win/env.ps1）
├── repo/
│   ├── LocalRepo/              测试本地仓（HAO_REPO）
│   └── RegisterRepo/           私服远程源数据（仅 haoreg）
├── test/
│   ├── suite/                 多文件集成套件（package main + 嵌套子包），**基线 1011** 行
│   ├── oldcase/               归档旧单文件用例（不再测试）；gc.hao 在此，做 GC 特性时加回
│   └── syntax.hao             语法解析覆盖（只 parse）
│                              ※ 对外示例请写/改 **haolang-example/**，勿把 suite 当教程
├── output/hao.exe              编译器产物
├── target/                     发行包 + **test/** 测试临时区（规则 7）
└── VERSION                     版本唯一来源（CMake 注入 HAO_VERSION）
```

### 改哪里 / 新增哪里（三向指引）

| 我要做什么 | 改哪个文件 |
|-----------|-----------|
| 改语法（新增关键字/结构） | `src/ast/HaoLangLexer.g4` + `HaoLangParser.g4` → `antlrgen` 重新生成 |
| 改 IR 生成逻辑（表达式/类/控制流/异常/闭包） | `src/irgen/IRGenExpr.cpp` / `IRGenClass.cpp` / `IRGenControl.cpp` / `IRGenExcept.cpp` / `IRGenLambda.cpp`（RMGen 类成员，任意 .cpp 定义） |
| 改类型系统/符号表 | `src/sema/Type.h` / `SymbolTable.h` |
| **新增运行时 C 函数** | 新建 `stdlib/runtime_<name>.c`（`build_runtime.ps1` 自动发现，无需改脚本），跨 C 边界返回字符串务必 gc_alloc+拷贝 |
| **新增标准库 .hao 包** | 新建 `stdlib/src/<pkg>/<pkg>.hao`（`package <pkg>;` + `public extern func` 声明 C 符号），发行版 `script/win/package.ps1` 自动拷整个 `stdlib/src` |
| 新增语言测试 | 写单文件临时验证 -> 合并进 `test/suite/` 对应模块（命名去冲突）-> 单文件移 `test/oldcase/`；跑 `bash script/test.sh --rebuild-all` 更新套件基线行数 |
| 改链接/库搜索 | `src/driver/DriverLink.cpp` |
| 改包/import 解析 | `src/driver/DriverResolve.cpp` |
| 升级版本 | 改根目录 `VERSION`，重跑 `cmake -S . -B build` |

---

## 第 8 章 · 构建与测试

### 构建编译器

```powershell
# 新机器一次性：powershell -File script\setup_env.ps1
. .\env.ps1                 # 加载 PATH（lib/llvm、scoop shims、output）
cmake --build build --parallel 8   # 改了 C++;新增 .cpp 需先重跑 cmake 配置
# 改了 .g4：antlrgen   或 java -jar lib/antlr4/antlr-4.13.2-complete.jar ...
```

### 编译运行时

```powershell
powershell -ExecutionPolicy Bypass -File script\win\build_runtime.ps1            # 宿主 Windows
powershell -ExecutionPolicy Bypass -File script\win\build_runtime.ps1 -Target linux-amd64  # Linux
```

### Windows CRT 最小集（打包 / 无 VS 目标机必需）

```powershell
powershell -ExecutionPolicy Bypass -File script\win\fetch_winlibs.ps1 -Target win-amd64
# win-arm64（需本机 VS ARM64 工具）:
# powershell -ExecutionPolicy Bypass -File script\win\fetch_winlibs.ps1 -Target win-arm64
```
### 跑全部测试（多文件套件 `test/suite`，基线 **1011** 行，全退出码 0）

```bash
bash script/test.sh                 # 增量：build+run test/suite + syntax parse
bash script/test.sh --rebuild-all   # 强制全量重编（改编译器/运行时/stdlib 后）
powershell -ExecutionPolicy Bypass -File script\win\negcheck.ps1   # 反向：须 28/28 编译拒绝
```

> 语言特性/标准库/系统库全部收敛进 `test/suite/`。
> 旧单文件归档 `test/oldcase/`（不再进 suite；`net.hao` 等可作专项冒烟）。
> 工作流：单文件临时验证 → 合并进 suite（命名去冲突）→ 移 oldcase → `--rebuild-all` 更新基线行数。
> **改 stdlib / 语法后务必 `--rebuild-all`**（增量不查 stdlib `.hao`）。

**网络专项**：`output/hao.exe run test/oldcase/net.hao`（不依赖 SDK `.lib`）。

**外部库测试**（用户侧 `@link`/`-l`，需本机导入库，**不是** stdlib 路径）：
```bash
output/hao.exe run test/extc/extc.hao --link test/extc/extc.c
output/hao.exe run test/winsys.hao                                 # 用户 @link("ws2_32")，需 SDK
output/hao.exe run test/linkpath/linkpath.hao
output/hao.exe run test/autofind/autofind.hao
HAO_LDFLAGS="-lws2_32" output/hao.exe run test/sysenv.hao
```

**冒烟**：`hao build test/suite --target linux-amd64 -o output/suite-linux` → 静态 ELF（`7f 45 4c 46`）。

### 打包发行版

仅宿主 **`win-amd64`** 可打包；其它 `-Target` 会失败并说明原因。  
`examples\` ← 整树拷贝 **`haolang-example/`**（含 `01-hello` 等）；`docs\` ← `hao语法.md` + `hao命令.md`；不拷贝 `test/suite` / `oldcase`。

```powershell
# 仓库根；完整前置 → 打包
. .\env.ps1
haobuild
powershell -ExecutionPolicy Bypass -File script\win\build_runtime.ps1
powershell -ExecutionPolicy Bypass -File script\win\fetch_winlibs.ps1 -Target win-amd64
# 终端不要 cwd 在 target\...\bin（会锁目录删不掉旧包）
powershell -ExecutionPolicy Bypass -File script\win\package.ps1 -Zip
# 调试可加 -SkipSelfCheck；自检探针在 target/test/package_selfcheck/（跑完清理）
```

产出：`target\win-amd64-haolang-<ver>\`（或被占用时的 `*-fresh`）+ 可选 `.zip`。  
目录必须含：`bin\hao.exe`、`stdlib\src\`（含 `net` 等）、`lib\llvm\bin\`、`lib\sysroot\win-amd64\lib\`（CRT 六件套含 **uuid**）、`examples\`、`docs\`。

### 调试手段

- `output/hao.exe emit test/xxx.hao` 看 LLVM IR。
- `hao run test/xxx.hao -v` 看 clang 链接命令（应含 `-L...\lib\sysroot\win-amd64\lib`）。
- `hao env` 看 clang / 运行时库 / **标准库源码** / Win CRT 路径。
- 查 obj 是否误链系统导入库：`llvm-readobj --coff-directives stdlib\runtime_net.obj`（应无 ws2_32；全库应无 `U __imp_*`）。
- 模拟无 VS 链接：只用 `-libpath:lib\sysroot\win-amd64\lib` 调 `lld-link`，不应再找系统 SDK 路径。

---

## 第 9 章 · 能做 / 不能做（已知限制）

### 9.1 长期限制 / 未排期（不是「将就债」）

**能做**：见第 3 章 + 第 4 章。

**不能做 / 尚未做**（中长期能力，勿与 9.2 混为一谈）：
- Linux **编译器分发包**要等自举后原生 `hao`（用户程序交叉编译已支持）。
- **GC（v0.55.10）**：见 §5.4 / 契约。**排期**：concurrent sweep / span。**架构**：禁 SuspendThread；shadow 精确根；非移动式。
- **haoroutine / channel / select**：基础 + select 已交付；载荷仍为 Long；select 为协作轮询（非内核多路 wait）。
- 自动属性 `{ get; set; }`、接口默认方法、接口继承接口、泛型约束 `where T : Speaker`：只解析或未实现。
- **反射**：类型自省 / 字段读写 / 注解 / **方法 invoke** 已有；**运行时动态定义类/成员**需解释器/VM，后续。
- 多文件/包：**整盘编译**（无 `.a` / 增量编译）——能力债，见 9.2-F 是否本批要动。
- 用户显式 `@link` 系统库仍可能需要本机 SDK 导入库；**`import net` / `import thread` 等 stdlib 不需要**。
- Windows 无 VS 机器依赖发行包内 CRT 最小集；缺 `lib/sysroot/win-*/lib` 会链失败。
- **无** Int↔Integer / String↔lang.String 隐式自动装箱（需显式 `valueOf`）。
- 位运算：整数族 + 一元 `~` + 移位掩码；**`lang.Bit`** 实例为 Long，静态 API 有 Int/Long 重载；**无** Char。
- **json / regex**：**v0.28 已交付**；不做 JSONReader/Writer 流式与 `@JSONField` 全量注解。
- **已废除**独立 `bits`/`path`/`io` 包——能力在 `lang.Bit` / `os`（含 FileStream）。
- **允许** `package math` + `class Math`（旧「会崩」说法已证伪并修复）。
- 并发关键字定名 **`haoroutine`**。
- darwin 目标不能随包分发 Apple SDK。
- 已有全套包装类（含无符号）；**有 Char**（码点）；无字段级紧凑打包（对象字段仍 8 字节槽）。
- **不做** i128/u128；UIntPtr 宽度本宿主固定 64；**String = ptr→HaoString**（非栈 fat；data 仍 nul 结尾供 FFI）。
- **IO/HTTP / MVC 扫描**：**v0.28～0.31 已交付**；**v0.49** 模板/JSON coerce/staticFiles。
- **MVC 仍缺（非清债批）**：路径变量 `{id}`、内容协商、方法参数自动绑定、真正的 IoC。

### 9.2 将就债清单（指针）

> **唯一正文**：[`docs/坑债.md`](docs/坑债.md) 第二节。

当前摘要：A～E 已于 v0.33 清零；**G** 于 v0.37；**H**（hao test 跑 main）于 v0.42 清零。延期项仅 **F**（增量编译）见坑债。
表空则开第 10 章下一批。修完一项从坑债表删除，并在踩坑表补「正式解法」行。

## 第 10 章 · 接下来要做

> **当前阶段（用户指定）**：**暂停** GC monitor 压测与功能冲刺；进入**项目优化 / 架构优化改造**。  
> 大方向（中长期仍有效）：架构对齐方法论 → 文档治理 →（后移）Netty NIO / GC concurrent sweep → 自举。  
> 一批 = 可验收一块；每块绿后立刻按文档治理更新（规则 1～2，**中文**）+ 删 `.bak`（规则 12）。  
> **新会话若未指定任务 → 直接开干本节「🔥 下一批」。**

### 已完成（摘要）

- ✅ **v0.55.49**：定位发现十五期 A13/D15/U17/V11/L15（unwind TRACE；fn/this dbg.value；catch-finally-throw；ext 毒针；坑债 DJ）
- ✅ **v0.55.48**：定位发现十四期 A12/D13/D14/U16/L14（grow_spill TRACE；lambda/select 薄 DI；nested-catch；坑债 DI）
- ✅ **v0.55.47**：定位发现十三期 A11/D12/U14/U15/L13（sticky/block TRACE；catch dbg.value；haoroutine/when×for try；坑债 DH）
- ✅ **v0.55.46**：定位发现十二期 A10/D11/U12/U13/L12（pin/enter TRACE；for 迭代 dbg.value；ctor/静态方法 try 根冒烟；坑债 DG）
- ✅ **v0.55.45**：定位发现十一期 D10/A9/V10/U11/L11（字段复合 dbg.value；unpin/leave TRACE；C 叶毒针；catch-only；坑债 DF）
- ✅ **v0.55.44**：定位发现十期 D9/V9/A8/U10/L10（下标 dbg.value；GPR VERIFY；recycle TRACE；方法 try 根冒烟；坑债 DE）
- ✅ **v0.55.43**：定位发现九期 V8/D7/D8/A7/L9（refl_i64 VERIFY；复合/字段 dbg.value；acquire TRACE；坑债 DD）
- ✅ **v0.55.42**：定位发现八期 U8/V7/D6/L8（Unit-lambda ret；remset VERIFY；赋值 dbg.value；坑债 DC）
- ✅ **v0.55.41**：定位发现七期 U7/V6/D5/L7（线程×try；再抛多层；pin VERIFY；薄 dbg.value；坑债 DB）
- ✅ **v0.55.40**：定位发现六期 U6/V5/A6/L6（break/lambda×try 根；VERIFY shadow_i；清槽 TRACE；坑债 DA）
- ✅ **v0.55.39**：定位发现五期 U5/A5/V4/L5（控制面 try 根；清槽 STRICT；VERIFY 重入；select !dbg；坑债 CZ）
- ✅ **v0.55.38**：定位发现四期 U4/V3/D4（嵌套 try 根；alloc VERIFY；catch/for+Except dbg；坑债 CY）
- ✅ **v0.55.37**：定位加深三期 U3/V2/D3（try×loop 根；VERIFY poison；`dbg.declare`；坑债 CX）
- ✅ **v0.55.36**：定位门禁 C0～T1（test.sh；VERIFY；dbg TLS；try 根；坑债 CW）
- ✅ **v0.55.35**：定位发现 D0～U1（`loc_smoke`/`spill_ir_smoke`；调用点 src=；`[lib]`；坑债 CV）
- ✅ **v0.55.34**：嵌套 while `stickyFloor`；`gc_block_scope_root_smoke` 多轮绿（坑债 CU）
- ✅ **v0.55.33**：定位加深 P0～P3 + G1 口径（`stack=`；套件双门禁；block 压力冒烟记债）
- ✅ **v0.55.32**：定位底座 L0 + R4L/R5/R3/D1/DOC/I2+（panic `src=`；Driver Diagnostic）
- ✅ **v0.55.31**：dbg 方法论补齐（按函数 DISubprogram / 方法·lambda loc / 命令 `-g`）
- ✅ **v0.55.30**：I0/I3 + 薄 I4（SourceLoc / `!dbg` / `-g`）
- ✅ **v0.55.29**：IROps 扩 API + I5 收口（口径：非零 emit）
- ✅ **v0.55.28**：I5 复查补遗 + Class/Lambda
- ✅ **v0.55.27**：IROps 收口 + I5 批4 Literal/Except
- ✅ **v0.55.26**：I5 批3 Expr（186→83）
- ✅ **v0.55.25**：I1b + I5 批1/2（Value/Control）
- ✅ **v0.55.24**：定位优先 Phase A（R1/R2/R4'/I2）
- ✅ **v0.55.23**：JSON Map/`contentLengthOf` 套件回归
- ✅ **v0.55.22**：CF 续（invokeObj 对标 Java）；monitor **约 1 天压测稳定**、峰值约 **10MiB**
- ✅ **v0.55.11～0.55.21**：STW/根/remset/while sticky/反射 pin 等 CF 续（详见时间线）
- ✅ **v0.55.10**：?. /装箱/皮带 + CE continue/finally 假死修复；基线 **1011**
- ✅ **v0.55.3～0.55.9**：循环 spill / 作用域 / 表达式根等（详见时间线）
- ✅ **v0.50～0.54**：并发标记、混合屏障、软 STW 等（详见时间线）
- ✅ **v0.36～0.49**：haoroutine/channel/select、包管理、Web、fmt/test（详见时间线）

### 架构方法论比对结论（本会话 · 步骤 1～2 加深）

> 对照两份方法论全文 + 实现抽查。**原则要遵守；示例函数名/Statepoint/`hao pkg` 非强制**；与 §5.15 / [`IR与GC契约.md`](docs/IR与GC契约.md) 冲突时以契约与已定决策为准。

#### 总判（一句话）

**目录/阶段结构已够工业可用，不必推翻。** runtime GC 行为大致已收口；**真正缺的是 IR 指令统一封装层**（文本 `.ll` 版 emitXxx），故 DebugLoc / 一处注入做不到；「作用域自动管根」与独立 Sema 为后置债，不挡 Debug 底座。

#### A. 已对齐 / 可接受差异（勿当缺陷重做）

| 域 | 现状 |
|----|------|
| 管线外形 | Lexer/Parser→（语义+IR 合体）→文本 LLVM IR→clang/lld→runtime |
| 包管理 | `mod/` + Driver；内核不写网络拉取；`haoproject.json` + `HAO_REPO`（≠ 文档示例 `hao pkg`/`HAO_PKG`） |
| GC runtime 入口 | 分配/`hao_gc_barrier`/`hao_gc_safepoint`/`hao_gc_root_*` 在 `runtime_gc.c`；禁 SuspendThread；**shadow 替代** LLVM Statepoint（契约） |
| IR 侧 GC 工具函数 | `emitHeapStore` / `emitSafepoint` / spill…（IRGen）；通用指令走 **`IROps`**（经转发） |
| 类型区分雏形 | `isReferenceType` + `isGcPointerType`；对象字段 `objectPtrBitmap` |
| 观测面 | `gc.GC` summary / heapBytes / markAbort…（非完整 Metrics 模块） |
| 构建交叉 | sysroot + 分目标 runtime；`package.ps1` 发行 |

#### B. 对照「统一收口」五大域（架构文档 §3）

| 收口域 | 判定 | 证据与缺口 |
|--------|------|------------|
| **GC 行为（runtime）** | 🔸偏 ✅ | 堆写屏障/分配/根 API 集中；channel 等仍有原生 `malloc`（非托管缓冲，可接受边界）；诊断埋点**未**内置进入口（开 GC 诊断不能只改一处） |
| **IR 生成 GC 相关** | 🔸半收口 | 屏障/对象 new/**safepoint** **已**走封装（**v0.55.24 I2**）；`rootGcOperand` 调用点遍布 Expr（数十处）——有公共函数，但**业务分支仍手选何时挂根** |
| **诊断 Diagnostic** | 🔸半收口 | IRGen `error()`→`diags_`；Driver/`main`/链接大量 `cerr`；无 JSON/颜色统一出口 |
| **Debug 埋点** | 🔸 | ✅ R1 Trace；✅ R4'；✅ I0/I3 `!dbg`（`-g`）；PDB/源码断点体验可再精修 |
| **IR 调试元数据** | ✅ 最小+薄 declare | `hao emit -g`：`!dbg` + CU/File/SP/Location + `llvm.dbg.declare`（参数/局部槽）；完整类型视图仍开 |

#### C. 对照 GC 清单核心哲学（清单 §一 / 终极方法论）

| 原则 | 判定 | 说明 |
|------|------|------|
| 可追踪 / 从根可达 | ✅ 主路径 | 契约 R1～R9；monitor 长稳 |
| 值/引用/裸指针边界 | 🔸 | 有判定函数；类型系统无完整 `GcManaged`/`IsUnsafePointer` 一等属性表 |
| **统一收口（GC 不散落）** | 🔸→❌ | runtime ✅；**IRGen 表达式/控制流分支仍散落挂根与清槽纪律**（违反「控制流只管跳转」） |
| **作用域优先（非人工 push/pop）** | ❌ | 现状正是人工：`rootGcOperand` / spill 池 / `clearLoopSpillSlots` / 块尾 `store null`；清单禁止「表达式里手写 gc_push」——我们用封装名，**语义仍是手插** |
| 不变量可校验 | 🔸弱 | 无 Debug 断言体系；堆校验/根水位匹配校验未产品化 |

#### D. GC 清单章节抽检（加深）

| 章节 | 判定 | 要点 |
|------|------|------|
| §三 精确布局 / refmap | 🔸 | 对象头+字段位图（≤32）；数组 esz；**无**完整 LLVM Stack Map；闭包 env 靠槽扫描非独立 refmap 表 |
| §四 根枚举 | 🔸✅ | 局部/参数/catch/when/env/静态根大体覆盖；临时根靠纪律非表达式作用域机 |
| §五 写屏障 | 🔸✅ | 堆字段/数组元素/静态/`emitHeapStore`；**局部栈槽赋值不经屏障**（shadow 根，设计可接受，但与清单「任何引用写入」字面不完全同） |
| §六 控制流 | 🔸 | 已补 continue/finally（CE）、块尾清、spill sticky；**仍靠各 gen* 分支手写**，非自动 |
| §七～八 闭包/字面量 | 🔸✅ | env `object_new`+屏障；字面量/模板有 root |
| §九 ABI / FFI | 🔸 | 实参 root；FFI **无**正式 Handle 体系（清单 §十四 ❌） |
| §十 Unwind | 🔸✅ | unwindGcRoot + root_unwind；与 finally 绑定有坑债史 |
| §十一 安全点 | 🔸 | 位置大体合规（入口/循环）；**调用前后非处处插**；无 StackMap；**emitSafepoint 已收口（I2）** |
| §十二 优化交互 | 🔸 | `-O2`+shadow；屏障未系统标 noinline/内存语义（文本 IR 靠 runtime 副作用） |
| §十三 并发 | 🔸✅ | 软 STW、线程注册、混合屏障；协作 safepoint |
| §十四 Handle | ❌ | 无局部/全局句柄 API |
| §十五 弱/软引用 | ❌ | 无；有 finalizer |
| §十六 OOM/大对象 | 🔸 | 有失败路径；非完整 OOM 诊断包 |
| §十七～十八 可观测/溯源 | 🔸 | 有 GC 指标；✅ Trace 开关 + 崩溃 GC 快照；❌ 尚无源码级栈/`!dbg` |
| §十九 禁止项 | 🔸 | 「正式方案禁止纯保守」——已 shadow 精确为主+双轨诚实边界；「异常须映射源码」——**未做到** |

#### E. 架构分层（架构文档 §二 / §十）

| 项 | 判定 |
|----|------|
| ANTLR 只构造 AST | ✅（语义不在 g4） |
| 独立 Sema 遍后再 IRGen | ❌（类型检查在 IRGen；**中期债，不阻塞 Debug/IR 收口**） |
| IRGen 按语法拆多 cpp | ✅ 保留（Expr/Control/Class/Lambda/Literal）——与工业编译器同构，**禁止为收口而拆目录/推翻** |
| IRGen 不改 AST / 符号表只读 | 🔸（ANTLR 树只读；符号在 IRGen 生命周期内自建自改） |
| 包管理与内核解耦 | ✅ |
| 诊断统一 | 🔸 |

#### F. Debug / IR 指令统一封装——能否做到？（对照外部建议 + 本仓库现实）

> 外部建议核心：**架构不用推翻**；只在 IRGen 内加一层 `emitAdd/emitLoad/…`，DebugLoc/GC/日志只改封装。  
> 本仓库差异：生成的是**文本 LLVM IR**（`IREmitter::emit(string)`），不是 `IRBuilder::CreateAdd`。原则相同，落点换成「类型化文本发射 + 可选 `!dbg`」。

| 能力 | 方法论/外部建议目标 | 当前 | 能否做到 | 怎么做（不推翻结构） |
|------|---------------------|------|----------|----------------------|
| IR 指令统一收口 | 所有 CreateXXX 经封装 | ✅ **同类指令**经 IROps；switch/setjmp 等有意裸 emit | ✅ **能** | 新代码走 `ops_`/转发；I3 只改 IROps |
| DebugLoc 一处注入 | 封装内 `setDebugLoc` | ✅ **文本** `IROps` + `finish` DI（`-g`） | ✅ | 默认关；`hao emit -g` 可见 |
| `.hao` 源码断点 | VS/lldb 对 `.hao` 下断 | ❌ | ✅ **能，分阶** | L1 元数据+链接 `-g` → L2 列/作用域/`DILocalVariable` → L3 调试器类型视图（后） |
| panic/异常映射源码行 | 栈含文件:行 | ❌ 仅英文/中文消息 | 🔸 **部分不依赖全量 dbg** | runtime 崩溃钩子先做；**精确行号**需 IR 元数据或手工 `__hao_loc` 表，故 **IR 封装层是前置** |
| GC 诊断只改入口 | Trace 在 `hao_gc_*` 内 | ❌ | ✅ **能，且应先做** | runtime 入口加 `hao_trace`/`HAO_GC_TRACE`；与 IR 封装**并行不互相挡** |
| 作用域自动临时根 | 控制流零 GC | ❌ | 🔸 **能，但是后做** | 不阻塞 Debug；属 GC 体验升级，排在 IR 指令收口之后 |
| 换 LLVM C++ API | Builder 直调 | 刻意不用（工具链） | ❌ **本阶段不做** | 文本 IR 是架构决策；封装层保持「后端可替换」接口形状即可 |

**结论**：扩展 Debug **可以做到**方法论要的「一处改、全链路生效」——**IROps + I0/I3/薄I4 已就位（v0.55.30～0.55.31）**；再精修局部变量 DI / 断点体验即可，不是目录结构错。

#### G. 对比后的优化方案（IR + Runtime · 底层优先 · 不互相绊脚）

> 原则：**先打地基能力，后挂体验**；各轨可并行（Runtime Trace ∥ IR 封装），不因「先文档后代码 / 先 Sema 后 Debug」卡住。  
> **不做**：推翻 `irgen/` 拆分、迁 LLVM C++ API、本阶段 Sema 大搬家、作用域根机大重构（后置）。

##### 轨 R · Runtime（可与 IR 轨并行）

| 序 | ID | 内容 | 验收 | 依赖 |
|----|-----|------|------|------|
| 1 | **R1** | `hao_assert` / `hao_trace`（env：`HAO_TRACE`/`HAO_GC_TRACE`）；Release 宏空开销 | 开关关=无输出；开=GC 一轮有迹 | 无 |
| 2 | **R2** | 把 Trace **埋进** `hao_gc_barrier` / `hao_gc_safepoint` / `hao_gc_root_push` / 分配入口（只改入口） | 业务 `.c` 无新增散落 printf | R1 |
| 3 | **R3** | Metrics 收口：现有计数走统一 `hao_metric_*` 或保持 `GC.*` 但内部一张表 | `GC.summary` 仍可用 | R1 可并行 |
| 4 | **R4** | **增强**现有 Win `hao_crash_uef`：GC phase/堆摘要；panic 同源；**另** Linux 钩子 | 崩溃 log 含 GC；非从零 | R1；**不**等 dbg |
| 5 | **R5** | panic 路径接钩子；OOM 日志带堆摘要（清单 §18.2 缩略版） | panic 非裸 fputs | R4 |

##### 轨 I · IR / IREmitter（Debug 与 GC IR 收口的共同底座）

| 序 | ID | 内容 | 验收 | 依赖 |
|----|-----|------|------|------|
| 1 | ✅ **I0** | `SourceLoc{file,line,col}` + gen* 从 ANTLR token 填 loc | 报错/封装能拿到位置 | 无 |
| 2 | ✅ **I1** | **指令封装层**（文本）：经 **IROps** | 新/改路径只走封装；套件绿 | I0 可后补 loc |
| 3 | **I2** | GC IR 专用封装收齐：`emitSafepoint` / 已有 `emitHeapStore`/`emitGcRootPush`/`emitObjectNew`；**全库消灭裸** `call void @hao_gc_safepoint` | rg 仅封装内出现该字符串 | I1 可并行先做 safepoint |
| 4 | ✅ **I3** | 封装内挂 `!dbg`（`-g`）；模块级 DI；按函数 DISubprogram | `hao emit -g` 可见 | I0+I1 |
| 5 | ✅ **I4** | CLI `-g` + clang `-g`；策略见 [`hao命令.md`](docs/hao命令.md) | 显式 `-g` 出调试信息 | I3 |
| 6 | **I5** | 渐进把高频 `em_.emit` 迁进封装（先 load/store/call/br，后 binop） | 裸 emit 计数下降；每迁一块测绿 | I1 |

##### 轨 D · 诊断（薄、可插空做）

| 序 | ID | 内容 | 验收 | 依赖 |
|----|-----|------|------|------|
| 1 | **D1** | Driver/`main`「错误:」→ `DiagnosticEngine`（成功信息可保留 cout） | 编译失败格式统一带文件:行 | 无；不挡 I/R |

##### 轨后置（有底座再做，避免绊脚）

| ID | 内容 | 为何后置 |
|----|------|----------|
| **G1** | 临时根「作用域机」减少 `rootGcOperand` 散点 | 依赖 I1 习惯与稳定性；大改控制流 |
| **G2** | Sema 与 IRGen 拆遍 | 搬家式；与 Debug **无硬依赖** |
| **G3** | FFI Handle / 弱引用 | 产品能力，非 Debug 底座 |
| **G4** | concurrent sweep | 排期 |

##### 推荐落地序（底层先 · 少等待）

```
并行启动：
  R1 → R2 → R4 → R5
  I2(emitSafepoint) → I1(指令封装骨架) → I5(渐进迁移) → I0/I3/I4(loc+dbg+链接)
空窗：D1、方法论文档对照注
后置：G1 → G2；G3/G4 按需
```

**验收总闸**：每完成一个 ID → `.bak` → 测绿（套件 ≥1011，必要时 negcheck）→ 按文档治理更新 → 删 `.bak`。

#### H. 代码审计（对照结论 · 本会话）

> 抽样：`src/irgen`、`src/driver`、`src/main.cpp`、`src/sema`、`stdlib/runtime_*.c`。用于校正比对表述并冻结改造任务。

##### 审计与比对结论的出入（必须改口径）

| 比对原文 | 代码事实 | 修正 |
|----------|----------|------|
| 「无崩溃钩子 / R4 从零做」 | Win 已有 `hao_crash_uef`（`SetUnhandledExceptionFilter`）+ `hao-crash.log`（RVA/GPR/原生栈）；随 `hao_win_ensure` 安装 | **R4 降级为增强**：补 GC phase、与 panic 统一入口、Linux 对等；非从零 |
| 「Debug 埋点基本未建」全称 | 崩溃现场**半套**已有；仍无 Assert/Trace/`!dbg`/源码行 | 表述改为：✅ Trace/Assert/R4'/`!dbg`（`-g`）；源码断点体验可再精修 |
| 「em_.emit 约 500+」 | 各 cpp 计数合计约 **513** 次（Expr100+Control100+Literal80+Value77+Except66+Class47+Lambda25+IRGen18） | 数量级正确，可写 **~510** |
| safepoint 裸 emit | **10 处 / 6 文件**：Control×4、Class×2、Literal×2、Lambda×1、IRGen×1；**无** `emitSafepoint` | 与结论一致 |
| barrier/root/object_new | **仅** `IRGenValue.cpp` 封装内 `em_.emit` 拼 call（+ Emitter declare） | 比「半收口」略好：**定义已收口**；缺口是 safepoint 与通用指令层 |
| `rootGcOperand` | Expr **45** + Literal7 + Control4 + Class2 + Lambda 等，合计约 **60+** 调用 | 与「人工散点」一致 |
| `sema/` | 仅 `Type.h` / `SymbolTable.h` / `Diagnostic.h`，无 Sema 遍 `.cpp` | 与结论一致 |
| Driver `-g` | ✅ CLI `-g`；链接条件加 clang `-g`；见 hao命令.md | 与 I4 一致 |
| Linux 崩溃 | ✅ `runtime_posix_crash.c`（SIGSEGV/ABRT/BUS/FPE/ILL → log+GC+src）；Win 仍 UEF | R4L 已对等（需 linux 冒烟） |

##### 审计快照校正（v0.55.32 · 相对初审）

- ✅ 已有 `HAO_ASSERT` / `hao_trace` / `-g`→`!dbg`/`DILocation`；L0b panic 带 `src=file:line:col`
- 堆字段写走 `emitHeapStore`/`emitGlobalGcStore`；局部/清 null/vtable/fnptr 槽可裸 `store`（设计可接受）
- GC 托管分配走 `gc_alloc*`；channel/thread/regex 等原生 `malloc` 为非托管缓冲
- IRGen `error`→带文件路径的 `diags_`；Driver 编译/链接失败走 `DiagnosticEngine::toolError`；CLI 用法可留 cerr
- 包管理在 `mod/`，编译内核无 HTTP 拉取
- **文本 IR**（非 LLVM C++ Builder）+ **shadow 精确根**（非 Statepoint）为本仓库既定工具链；方法论工业清单里的 Statepoint 项对本仓库映射为 shadow/safepoint 封装，不强制改栈图元数据

#### I. 改造任务清单（审计冻结 · 可勾选）

> 底层优先；R∥I 可并行。每项：触点 → 验收。

| # | ID | 优先级 | 任务 | 主要触点 | 验收 |
|---|-----|--------|------|----------|------|
| 1 | ✅ **I2** | P0 | 新增 `IRGen::emitSafepoint()`；替换全部 10 处裸 call | `IRGen.h` / `IRGenValue.cpp`；Control/Class/Literal/Lambda/IRGen.cpp | `rg "hao_gc_safepoint" src/irgen` 仅封装+declare；套件绿 |
| 2 | ✅ **R1** | P0 | `hao_assert` / `hao_trace`（`HAO_TRACE`/`HAO_GC_TRACE`）；Release 空 | `runtime_debug.c` + `runtime_internal.h` | 关=静默；开=有输出 |
| 3 | ✅ **R2** | P0 | Trace 埋入 GC **冷**入口（collect/safepoint park；禁热路径刷屏） | `runtime_gc.c` 仅入口 | 业务文件无新散落 printf |
| 4 | ✅ **I1a** | P0 | 指令封装骨架：`emitLoad`/`emitStore`/`emitCall`/`emitBr`/`emitCondBr`/`emitRet`（文本） | `IRGen.h` + `IRGenValue.cpp` | 新代码纪律；旧代码可并存；套件绿 |
| 5 | ✅ **I1b** | P1 | 补 `emitBinOp`/`emitICmp`/`emitPhi`/`emitAlloca` | `IRGen.h` + `IRGenValue.cpp` | 同 I1a |
| 6 | ✅ **I5** | P1 | 同类指令已迁；余 switch/setjmp（允许） | 分批 | 每批测绿；**非**要求零 `em_.emit` |
| 7 | ✅ **I0** | P1 | `SourceLoc` + 关键 gen* 填 loc | `SourceLoc.h`；Expr/Control/Function | 封装能带 loc |
| 8 | ✅ **I3** | P1 | 封装内可选 `!dbg`；模块 DI 元数据 | **`IROps`**；`finish()` | `hao emit -g` 可见 `!dbg` |
| 9 | ✅ **I4** | P1 | CLI `-g` + clang `-g`；[`hao命令.md`](docs/hao命令.md) | `DriverLink`；main | 带 `-g` 出调试信息 |
| 10 | ✅ **R4'** | P1 | **增强**现有 Win 崩溃钩子：写 GC phase/heap 摘要；panic/`hao_panic_*` 走同一上报 | `runtime_winapi.c` / `runtime_panic.c` / `runtime_debug.c` | 崩溃 log 含 GC 字段；panic 同源 |
| 11 | ✅ **R4L** | P1 | Linux：`sigaction` 崩溃钩子对等（地址+粗栈） | `runtime_posix_crash.c` | 代码审阅 + Win 不回归；linux 目标有 lib 时冒烟 |
| 12 | ✅ **R5** | P2 | OOM/分配失败走 `hao_report_fatal`（含请求大小 + 堆摘要） | `runtime_gc.c` | 文案含字节数；fatal 写 snapshot |
| 13 | ✅ **R3** | P2 | Metrics 表化（`hao_metric_*`）；`GC.summary` 语义不变 | `runtime_metrics.c`；`GC.hao` | summary 格式不变 |
| 14 | ✅ **D1** | P2 | Driver 编译/链接「错误:」→ Diagnostic（CLI 用法可留 cerr） | `Driver*.cpp`；`Diagnostic::toolError` | `hao:0:0: 错误:` |
| 15 | ✅ **I2+** | P2 | 纪律：禁止新代码在 gen* 裸拼 `@hao_gc_safepoint`（除封装） | `IRGen.h` 注释 | `hao_gc_safepoint` 仅 Value+Emitter |
| 16 | ✅ **G1** | 口径 | 既有 block/loop spill = 作用域临时根机；禁盲目语句级 expr 清槽 | Control；坑债 CT | 套件 1011；`gc_block` 压力 AV 记债 |
| 17 | **G2** | 后置 | Sema 遍抽出（类型检查搬家） | 新 `src/sema/*.cpp`；IRGen 只读 | 规则 4 分块绿 |
| 18 | ✅ **DOC** | 插空 | 方法论文本 IR/shadow 对照；校正 §H 过时句 | 本文件 §H；方法论注 | 口径与代码一致 |
| — | G3/G4 | 排期 | Handle / 弱引用 / concurrent sweep | — | 非本清单阻塞项 |

##### 建议开工序（审计后）

```
1 → 2 → 3（或 1∥2）→ 4 → 6 分批 → 7 → 8 → 9
10∥11 → 12 → 13 → 14 → 18 插空
16 / 17 后置
```

### 🔥 下一批（开新会话默认任务）

| 步骤 | 做什么 | 注意 |
|------|--------|------|
| ✅ | 比对 + Debug 可行性 + **代码审计校正**（§H/§I） | 已完成 |
| ✅ | **Phase A**：I2 + R1+R2 + R4'（**v0.55.24**） | 套件 1011 |
| ✅ | **IROps** + I5 收口（**v0.55.29**） | 同类经封装；允许 switch/setjmp |
| ✅ | **I0/I3 + 薄 I4**（**v0.55.30**） | `-g` 可见 `!dbg`；默认关 |
| ✅ | **dbg 方法论补齐**（**v0.55.31**） | 按函数 SP；方法/lambda；命令文档 |
| ✅ | **定位底座 + 剩余收口**（**v0.55.32**） | L0/R4L/R5/R3/D1/DOC/I2+ |
| ✅ | **定位加深**（**v0.55.33**） | P0～P3；stack=；G1 口径；block 压力债 |
| ✅ | **嵌套 sticky**（**v0.55.34**） | stickyFloor；block smoke 多轮绿 |
| ✅ | **定位发现**（**v0.55.35**） | D0～U1；两脚本门禁 |
| ✅ | **定位门禁**（**v0.55.36**） | C0～T1；test.sh+VERIFY+TLS |
| ✅ | **定位加深三期**（**v0.55.37**） | U3/V2/D3；多层 try 根；VERIFY 坏根；薄 declare |
| ✅ | **定位发现四期**（**v0.55.38**） | U4/V3/D4；嵌套 try；alloc VERIFY；catch/for dbg |
| ✅ | **定位发现五期**（**v0.55.39**） | U5/A5/V4/L5；控制面 try；STRICT；VERIFY 重入；select !dbg |
| ✅ | **定位发现六期**（**v0.55.40**） | U6/V5/A6/L6；break/lambda×try；shadow_i；清槽 TRACE |
| ✅ | **定位发现七期**（**v0.55.41**） | U7/V6/D5/L7；线程×try；再抛；pin VERIFY；薄 dbg.value |
| ✅ | **定位发现八期**（**v0.55.42**） | U8/V7/D6/L8；Unit-lambda ret；remset；赋值 dbg.value |
| ✅ | **定位发现十五期**（**v0.55.49**） | A13/D15/U17/V11/L15；unwind TRACE；fn/this dbg；catch-finally-throw；ext 毒针 |
| ✅ | **定位发现十四期**（**v0.55.48**） | A12/D13/D14/U16/L14；grow_spill TRACE；lambda/select 薄 DI；nested-catch |
| ✅ | **定位发现十三期**（**v0.55.47**） | A11/D12/U14/U15/L13；sticky/block TRACE；catch dbg；haoroutine/when×for try |
| ✅ | **定位发现十二期**（**v0.55.46**） | A10/D11/U12/U13/L12；pin/enter TRACE；for 迭代 dbg；ctor/静态 try |
| ✅ | **定位发现十一期**（**v0.55.45**） | D10/A9/V10/U11/L11；字段复合 dbg；unpin/leave TRACE；C 叶毒针；catch-only |
| ✅ | **定位发现十期**（**v0.55.44**） | D9/V9/A8/U10/L10；下标 dbg；GPR VERIFY；recycle TRACE；方法 try |
| ✅ | **定位发现九期**（**v0.55.43**） | V8/D7/D8/A7/L9；refl_i64；复合/字段 dbg；acquire TRACE |
| 🔥1 | 功能轨；或完整 Hao 类型/`dbg.value` 全覆盖 | — |
| — | （后置）G2 Sema；#16；Netty；sweep | — |

### 包管理路线（接 5.15）

| 顺序 | 层 | 状态 |
|------|----|------|
| 1 | 第 0 层：清单 + localReferences + `mod init` | ✅ v0.38 |
| 2 | 第 1 层：精确版 + cache + lock + file 仓 | ✅ v0.39 |
| 3 | 第 2 层 A1～A3：semver / 图 / HTTP | ✅ v0.45～0.47 |

### 中期

- I5 迁完高频指令；I3 完善 DI；G1 作用域临时根；G2 Sema 拆分（不挡前面）
- select 真等待；泛型 channel；MVC 绑定/IoC；增量编译（债 **F**）
- GC concurrent sweep / span（停顿排期）

### 长期

- **自举**（`hao-build-lang/`）

### 推荐顺序

✅ **比对结论** → ✅ **Phase A** → ✅ **I1a/I1b/IROps** → ✅ **I5** → ✅ **I0/I3/I4 dbg** → ✅ **G1 口径** →（后置）G2 → 功能/sweep → 自举。

---
## 附：批末文档检查清单（交给新会话 / 自己收工用）

收工前逐项勾选；**全部完成才能认为本批/本会话结束**。

- [ ] 对用户回复与两份文档叙述均为**中文**（规则 1）
- [ ] 改源码前已建 `.bak`；验收绿且文档写完后**已删除全部 `.bak`**（规则 12）
- [ ] 已写清「做完了什么」（含验收行数）+「踩坑：现象→原因→解法」（规则 2）
- [ ] 若本批有权宜之计：已标「将就」并写入 [`docs/坑债.md`](docs/坑债.md)（禁止假装闭环）
- [ ] 若升版：`VERSION` 已升；`cmake -S . -B build` 后 `hao version` 一致；若不升版：抬头/README 版本叙述仍正确
- [ ] 本文件：抬头版本/基线；第 0.4～0.5；**时间线合订**有本版专节；**坑债**有本批坑；第 10 章下一步正确
- [ ] `README.md`：版本、进度、限制、下一步已同步；时间线/坑债以链接指向 `docs/`（无过时基线）
- [ ] 套件 ≥ **1011** 行全绿；改 runtime/stdlib/语法用过 `--rebuild-all`
- [ ] 本批新/改 public API 注释符合 **5.14**
- [ ] 动可空/类型语义时：`script/win/negcheck.ps1` 全拒（当前 **28/28**）
- [ ] 若动链接/打包：`fetch_winlibs` + `package.ps1` 自检通过
